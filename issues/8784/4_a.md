It is indeed rather confusing. Unfortunately the warning it self is issued outside the domain. The domain gets the first chance to resolve it, and if unsuccessful the ``missing-reference`` is triggered, giving extensions like intersphinx the chance to resolve it (see ``ReferencesResolver`` in ``sphinx/transforms/post_transforms/__init__.py``).
So both the original domain and the extensions should get a chance to augment the warning. Perhaps ``resolve_xref`` could return a "reason or hint text" as well? @tk0miya, do you have any thoughts on this? would this be interesting for other domains than the C++ domain?