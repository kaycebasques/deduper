Is ``xbt::signal`` declared somewhere? When I try with the following I only get nitpicky warnings about ``xbt``.
```rst
.. default-domain:: cpp

.. var:: xbt::signal< void(const Actor &, const Host &previous_location)> Actor::on_host_change


.. class:: Actor
.. class:: Host
.. class:: template<typename F> xbt::signal
```
When a template specialization can not be found, it will fall back to the primary template for lookup (some old discussion is at #2057).
While #3320 may seem conceptually similar, the domains function very differently.