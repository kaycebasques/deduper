These are related, but not duplicates.

#701’s original request is relative URLs in a toctree to static files (`href="../some/path.html"`).

Out-of-tree source documents, paths above the Sphinx source root (`../../common/foo.rst`), were discussed later in that thread, but that is really a separate feature. This issue is the better tracker for documents outside the source tree , including how relative paths in the included document should resolve, as outlined above.