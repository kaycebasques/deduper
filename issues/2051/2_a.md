In my short investigation:

It is happened only on `latexpdfja`. On the other hand, on `latexpdf`, it works fine.
And more, it looks fine for me to replace tilde mark with `\textasciitilde`.

But I can not judge which should be better to resolve this on either Sphinx or pygments.
