When my rest file contains the lines below

.. code-block:: bash
  :linenos:

  #! /bin/bash
  BIN=˜/bin/a.out
  rm WAVE
  for i in 3.5 3.6 3.7 3.8 3.9 4.0 4.1 4.2 4.3 ; do
  cat >POST <<!
  .
  . 
  ., 
the html output is good.  But the latexpdfja (at least latexpdfja, i have not tested latex nor latexpdf)  is very bad, because it shows:

  #! /bin/bash
  BIN=^^cb^^9c/bin/a.out
  ….

I mean the translation of “~” is insufficient.  Could you fix it soon? 
