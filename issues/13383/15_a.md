> It seems this was intended to be a "named hyperlink reference with embedded **alias**" (mind the trailing underlina after `duplicate`):
> 
> ```
> `something <duplicate_>`_
> ```

Sorry, yes - I made a mistake here; it was not my intention for the two references of this form to be to external content.

Additionally, the issue in the Linux docs reported here did use the Sphinx `:ref:` role, while my attempt to replicate it did not.

Because the bugfix in the Linux docs was to remove the use of duplicate target names -- something that another developer could, I expect, innocently introduce by mistake, and that could be difficult to notice during code review -- I opened this issue with the intent to raise a warning when such targets are declared.

It seems I should take some time to better understand the hyperlinking syntax and semantics, and also create a more accurate minimal repro case.