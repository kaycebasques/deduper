By default captions are placed below figures, but above tables and code-blocks. I'd like the option to change this. In particular, I use code-blocks for sample input or output, and the caption to describe them, rather than as a filename, and I'd like these captions below the blocks.
