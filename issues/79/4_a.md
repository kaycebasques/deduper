> It may also be better to leave out that //object// is inherited from and any methods from it.

+1. IMO showing methods inherited from `object` adds a lot of noise to what is otherwise a very useful feature.

How about making `:inherited-members:` hide methods inherited from `object` by default, but letting users opt into showing those if actually desired?