The regex includes "\s" ie whitespace characters, which in python is defined as `[ \t\n\r\f\v]` and unicode whitespace.
I'm not sure where "beginning of string" is handled but just creating an file with just
```
"foo"
```
and running `rst2html --smart-quotes=yes` on it shows that this case is also correctly handled.

So I don't believe it's (solely) a docutils issues as described above...