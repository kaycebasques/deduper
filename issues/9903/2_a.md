Looks like the situation is now a bit different: autosummary will emit a warning since the member does not exist on the current class (it's defined in a base). No stub is generated and the reference is broken.

I'm not sure how to workaround that since the template responsible to generate the member listing simply does:
```
   .. autosummary::
   {% for item in methods %}
      ~{{ name }}.{{ item }}
```
which is wrong. It should be something like `{{ class_where_item_is_defined(name, item) }}.{{ item }}` but I could not find how to inject custom Python code there. The code itself would simply have to crawl `inspect.getmro(cls)` until it finds the member and emit a reference to that, and possibly generate the stub for it (if stubs are all stored in a flat folder, generating multiple the same stub should not lead to an explosion of sources)

EDIT: alternatively, autosummary could expose a variable to the template containing full member names (including defining class and module) that can be used directly, without having to join them in the template. This would also be fully backward compatible.