Unfortunately, this will not be fixed from #8781, because `tabulary` already by default hyphenates first word in table cell. (but #8781
would improve with `tabular` and `longtable`)

Indeed (not surprisingly?) `xquery` has no hyphenation point for TeX and (surprisingly?) `columns` does not have either, but `column` has:

```
$ etex
This is pdfTeX, Version 3.14159265-2.6-1.40.21 (TeX Live 2020) (preloaded format=etex)
 restricted \write18 enabled.
**\relax
entering extended mode

*\showhyphens{xquery}

Underfull \hbox (badness 10000) detected at line 0
[] \tenrm xquery

*\showhyphens{columns}

Underfull \hbox (badness 10000) detected at line 0
[] \tenrm columns

*\showhyphens{filter}

Underfull \hbox (badness 10000) detected at line 0
[] \tenrm fil-ter

*\showhyphens{column}

Underfull \hbox (badness 10000) detected at line 0
[] \tenrm col-umn

*\showhyphens{query}

Underfull \hbox (badness 10000) detected at line 0
[] \tenrm query

*\bye
No pages of output.
Transcript written on texput.log.
```

With

```rst
========= ======== =======================================================
Tag       Required Description
========= ======== =======================================================
column    No       Columns of the content to return. Each content listing
                   has a set of defaults columns that make sense for that
                   particular content type. By default, these columns are
                   returned in addition to what was requested here.
filter    No       Return files containing the specified filter string
========= ======== =======================================================
```

and current 3.x (#8781 not merged) I get
![Capture d’écran 2021-01-29 à 11 42 27](https://user-images.githubusercontent.com/2589111/106265366-38bba400-6227-11eb-934b-945f8018f64e.png)

Seems only solution here is manual setting by user of `\tymin`, because as above shows output depend on existence of hyphenation points. We could possibly decide for a default larger than the one of tabulary (which is `15/72.27in`, i.e. a bit less than `0.53cm`.)