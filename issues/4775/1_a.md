Subject: extra `References` chapters will show up in `latexpdf` build

### Problem

I put all my references in one chapter so for the `html` build, the references will be shown in one place.
However, for the `latexpdf` build, I notice a chapter `References` with chapter number will show up with no references in it while another standalone chapter `Bibliography` will be attached to the end.

#### Expected results
I'd like the `References` page shown as it is now in the `html` build but the blank `References` chapter can be excluded from the `latexpdf` build.

### Reproducible project / your project
The RTDS webpage:
- http://suews-docs.readthedocs.io/en/latest/references.html

The PDF with extra but blank `References` chapter:
- https://media.readthedocs.org/pdf/suews-docs/latest/suews-docs.pdf

### Remarks
I know this "issue" is more like a question but I got nothing when I researched for a long time on how to achieve that.
