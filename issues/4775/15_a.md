@funkyfuture Direct usage of `xindy` is not the recommended way. Rather use the `make latexpdf` or `make -C <latex-build-dir> all-pdf` to build your project. The Sphinx contributed LaTeX makefile is adapted to the engine (lualatex) and language (German) and the LaTeX build repertory has specific xindy support files.