@funkyfuture 

Try with

```
    'preamble': r'''
\addto\captionsgerman{\renewcommand{\bibname}{References}}
    ''',
```

(or perhaps `\captionsngerman` if above fails, but I think polyglossia uses `german` contrarily to babel's `ngerman` --- not tested)

Side remark: if you use `:index:` with German words starting with non-ascii character, then I think indexing will trigger a build failure by lualatex as you have `latex_use_xindy = False`.