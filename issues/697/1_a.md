Attached is code which tries to "override" the indexing - the primary domain is set to be C, but despite that and the fact that I have:

```
#!c

class CIndex(Index):
    name = 'genindex'
    ... stuff ...
```

... the page created is still c-getindex.html.

The proposal is made of two (independent of eachother) parts:
# Change the default behavior to remove the 'c' bit if it is the primary domain.
# If the index's name is "genindex", then the page's style should match that of the "default" genindex - in other words, genindex should be a magic Index's name which also changes the index's theme.

Attached is my work so far.

---
- Bitbucket: https://bitbucket.org/birkenfeld/sphinx/issue/697
- Originally reported by: Tim Čas
- Originally created at: 2011-05-18T15:13:28.163
