> Apparently, the rendering has already been somehow wrong with Sphinx 8.1.3 as well, not hyperlinking the parameter list correctly:

Yeah, my original example also has the same problem IIRC, but Sphinx didn't use to show an error for it. From my understanding it's the `PAGE_FIT` that's tripping it up in your case and it may work with a different default.