> Duplicate of #10954?

Perhaps, though my guess is that here there's an attempt to fall back to the old (regex) parsing method which splits on whitespace, hence the error message you observe.

Are you using autodoc here or 'raw' reST directives? If the former, we could maybe look into improving parsing for types (the `np.int32` case), and perhaps it might be possible to do the same for other types with unrepresentable `repr`s?

I've just spent several months rewriting the core of autodoc so it should be easier to debug & run independently of core Sphinx, if you have time to do so. Look at `do_autodoc` in the tests for a starting point.

A