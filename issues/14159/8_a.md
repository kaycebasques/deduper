> My issue is that I am not even able to properly pinpoint this to a specific line as there is no further debugging information.

@stefan6419846 Personally, I just grepped for that string and commented them out until it worked to pinpoint the issue :grin:. I haven't had time to actually dig into this though, so for now it's just ignored using something like
```python
nitpick_ignore_regex = [
    ["py:class", r".*tuple\[float"],
]
```