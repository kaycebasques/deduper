> [@alexfikl](https://github.com/alexfikl) my first guess is that parsing fails due to `<factory>`. Can you try changing the default argument?

Yeah, just had the same idea and can confirm that setting it to something like
```
history: list[tuple[float, float]] = ()
```
no longer causes the problem. Duplicate of https://github.com/sphinx-doc/sphinx/issues/10954?