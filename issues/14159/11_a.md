There is some additional confirmation from #14547 (duplicate issue). The reporter was able to bisect this regression to 7838043f2 (#13536) and their `MutableMapping[..., ...]` dataclass example provides another reproducer for the same comma-splitting behavior.