The error points to `.. automethod:: __init__`.

This is a directive added by the "autodoc" Sphinx extension.

It seems the docstring of `AlgoDeliverEnum.__init__()` contains an unbalanced inline markup.
