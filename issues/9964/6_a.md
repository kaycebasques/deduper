if we switch from `xelatex` to `pdflatex`, and relaunch `make latexpdf` on a pre-existing earlier latex build, the latex build will probably raise an error due to left-over auxiliary files not being compatible. Simply iterate the `make latexpdf`. (or issue `make clean` first).