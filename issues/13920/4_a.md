@arthur-tacca I've avoided writing about the multiple return problem before because all the linked issues with the exception of your lack good examples, so I'd have to write the examples.

It will take me a few days before I can find the time to write a proper sum-up because there are variations to this problem that haven't been mentioned. However, I do not think that having multiple return types like in the exception example is a proper solution for Python code because any returning callable can only return a single tuple (with however many elements that tuple may have).

> This issue: Even in native RST, there's no way to specify multiple return types and descriptions.

In Python you can have only one return type, it's the description that can have several fields. If you check the linked threads they generally don't give good examples and none of them is close to exemplifying the problem as a whole.