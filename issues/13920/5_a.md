@electric-coder 

Do remember that the main point of this request is about putting the return type and value on a single line, *even when there is only one*, as shown in the first two images I posted. You're talking the situation with multiple items, grouped into a list; that's is definitely handy (I use it!) but, as I originally said, more of a bonus feature. I would hate to miss out on the main point because someone objected to that.

To be honest, if the "proper sum-up" you're talking about is a essay objecting to this feature (the grouping one) then I don't think it would be very constructive even if your points are valid. It's not outrageous to document return types of `tuple[Foo, Bar]` and `Foo | Bar` using a bullet point list "Foo - this is a foo; Bar - this is a bar" (hopefully with enough context to tell which of those two is meant). Whether it's actually a good idea is very subjective whether but it's really up to the documentation author to decide.