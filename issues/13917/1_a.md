### Describe the bug

I have a better alternative to `sphinx_highlight.js` in my docs (RtD's server side search + `:~:text=...` from snippets it returns), but I can't seem to disable `sphinx_highlight.js` from getting loaded.

Can it be made optional in `conf.py`?
