This is a bit annoying to fix because we should still support legacy configurations. One way I can think of is the following:

- If `version` is not a string and `release` exists, assume that `version` is not a real configuration value and fallback to `release`.
- If `version` is not a string and `release` does not exist either, use a default value instead (maybe?) Something else?