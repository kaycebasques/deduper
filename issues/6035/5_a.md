My large concern is how to keep health of env file. I can't say env data is enough healthy when errors are happened. So I also can't say dumping env data to disk is safety.
It might be good to dump env data on reading every document.

Note: we need to care for parallel build also.