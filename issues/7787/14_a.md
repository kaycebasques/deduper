Would love to see this implemented. Re this:

> It sounds good. But it is very difficult to support inheritance, f-string and other features. And I think this is not commonly used.

Perhaps I'm in a strange netherworld, but this is actually quite common in my experience. When we `@overload` functions, we typically _only_ document the overloads and _not_ the implementation, because the overloads do different things. So, as it stands, no documentation is rendered at all.

And as Eric pointed out, f-strings are already not allowed in docstrings.