Hmm, Windows does indeed not use UTF-8 as default encoding. But on the other hand, input() seems to have no issue with Umlaute normaly. E.g the following python script would work fine with 'ö' when run in a terminal:
```
a = input()
print(a)
```