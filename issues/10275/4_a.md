We could add a new table for this to `theme.toml`, which I think might be a nice solution?

e.g.

```toml
[theme_configuration_defaults]
html_permalinks_icon = "#"
```

The slight problem is that we currently load and initialise HTML themes much later than `Config`, so there isn't a nice way I can think of to actually update the values, save doing `value = new_default if value == default else value`.

I agree that this should be restricted to themes due to the risk of order-dependence if we allowed any extension to overwrite the defaults.

Thoughts?

A