Not sure if something has changes, but `-W error -W ignore::DeprecationWarning` does not return non-zero status code for:
```
    logger = logging.getLogger(__name__)
    logger.warning(f"[phpdomain] {message}", location=fromdocnode)
```

I am running Sphinx 4.5, not sure if something has changed meanwhile.