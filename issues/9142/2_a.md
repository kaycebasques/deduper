I think it's a responsibility of extensions. Sphinx-core should not collect the warnings emitted by non-standard way. So -1 for handling it automatically.

On the other hand, +1 for adding a helper like `transfer_warnings_to_sphinx_logging`. It must be helpful for extensions.