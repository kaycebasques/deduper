@bluetech The option seems to work for me, but unfortunately it only applies if the type name is documented.

It is arguably a good thing to document TypeVars. The default documentation for TypeVars shows covariance, bounds, etc. But it does mean that your project should define TypeVars in one place and reuse them, which is not the current convention.