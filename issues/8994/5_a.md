Right. I'm not sure what a good solution here because that information is a part of the public interface of definitions that use these TypeVars, and that information is not available otherwise, besides through naming convention (`T`, `S`, `T_co`, `Self`, `MyTypeT`, etc.). Personally, I think they should be documented, but marked as use-private interface-public.