Edit: I believe the hack I mentioned above using `type Foo = NewType('Foo' Bar) instead of just Foo = NewType('Foo' Bar)` is actually more of a workaround for a separate confounding issue relating to `autosummary`: #11552