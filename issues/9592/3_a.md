This has been _partially_ addressed by #11431 since v7.1.0 - SSL failures on HTTP HEAD requests will not be followed by an attempt to make an HTTP GET to the same host.

There isn't yet any memory of TLS failures on a per-host basis though.