So it seems the solution given in the linked stackoverflow remains the correct approach. However, in https://github.com/sphinx-doc/sphinx/blob/1b28e5724fc69e79458b9e9c696696bd27a753e3/sphinx/ext/autosectionlabel.py#L62 this is done in doctree-read hook rather than tweaking the class attributes.

This works, but I'd rather like to have a solution that takes the domain name into account, ie. being able to use eg. ```:http:ref:`routingtable` ``` instead of putting all labels in the std domain.


