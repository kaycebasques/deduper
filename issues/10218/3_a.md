I think "ec2 described-instances" is a part of command (a.k.a. sub-command). So it would be better to mark it up like following:

```
.. program:: was ec2 describe-instances
.. option:: --dry-run
```

Then, you can refer to the option via ```:option:`aws ec2 describe-instances --dry-run` ```.


+0 to add a new directive (scope) like "subprogram" as you suggested. I think it's enough to provide only "program" directive. But this idea can be acceptable for me.