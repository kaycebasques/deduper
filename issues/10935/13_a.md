> My comments were in response to the issue title "config to control the toc depth for a page and/or project": such a control already exists in the built-in themes (`globaltoc_maxdepth`) and it seems to be lacking in `furo`.

I think you're misunderstanding what this request is about. This user is asking for a limit on `toc` (i.e. in-page navigation), not `toctree` (i.e. site-wide navigation, assuming `titles_only` is passed). The maxdepth mechanisms you've mentioned apply to `toctree`s, not `toc`.