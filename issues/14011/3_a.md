My interpretation is that the documentation is a bit misleading. It is common that projects have a README in their root folder, but it's uncommon that this README is added to the docs as they typically have different scopes.

So I would say, the instruction is technically correct and behavior is intended. However, it's a bit misleading that we create a README and do not do anything with it. I see two possible improvements

- Remove de README part as it's not relevant to the docs
- Add a comment that this is the typical project structure, but that README is not part of the docs.

I'm inclined to go for the first one.

We could / should instead add the text of the README to `index.rst`.

