I have the same issue - for formal QA, documents must be in a very particular format specific to my organization. In the past I have used Doxygen and heavily modified its default document style to customize the title page, headers, footers, etc. (for reference: https://gitlab.com/apthorpe/sofire2/-/tree/master/doxygen?ref_type=heads) I attempted to use a `latex_document` tuple and `latex_additional_files` to import a renamed version of `sphinxmanual.cls` (changing only the file name and arguments to `\ProvidesClass`) but something is not getting set correctly. I've had no success with `latex_docclass` (documentation doesn't really explain how to use it).

Most of the modifications I need are small changes to the default documentclass (onepage, all arabic page numbers, etc.) and the only path forward seems to be to manually replace `_build/latex/sphinxmanual.cls` between running Sphinx and rendering with LaTeX.