I only sparsely worked with the latex backend so I cannot say for sure that we could extract a minimal set of macros for rendering our documents. 

There are actually a lot of hard coded macros that could be customised but this requires going through the code base and locate them, in addition to document them.

Which aspect of LaTeX do you want to customise? 
