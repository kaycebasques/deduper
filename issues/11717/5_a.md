@picnixz , if i change my `conf.py` like this, i turn off most of the sphinx formatting.

```python
latex_additional_files = ["logo.pdf", "customStyle.sty"]
latex_theme = "howto"
latex_elements = {
    'classoptions' : 'oneside',
    'preamble': r"\usepackage{customStyle}",
    "fontpkg": "",
    "fncychap": "",
    "geometry": "",
    "maketitle": "",
    "releasename": "",
    "tableofcontents": "",
    "transition": "",
    "makeindex": "",
    "printindex": "",
    "fvset": "",
    }
```

I could probably optimize the dict above to remove anything that is off by default (instead of being explicit). Here are the remaining formatting items i want to turn off or edit:

* The sphinx caption on the figures and tables is "Fig. 1" and "Table 1" respectively. My custom template has a different text/caption: "Figure 4-1" and "Table 4-1" where 4 is the section number. I have code in my custom file to change it, but it looks like in `sphinxmessages.sty`, the caption styles are hard-coded?
* The color for all the titles is blue. I think i can figure out how to make it all black in my custom style file, but it would be good to revert to the latex default (black) if i could toggle a key somewhere in the `conf.py` and not worry about adding to my custom style file.
* I have to find a way to reconcile how sphinx uses references and glossaries versus how my style file does, but i think i will differ to sphinx here and turn off the references and glossary code in my style file.

I cant see anything else yet, but it looks like those changes to my `conf.py` got me halfway there.