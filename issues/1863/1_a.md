There is no table of contents in singlehtml if I select basic theme. I need simple html file with no side bar but with table of contents at the begining of the document.

I've tried to use reST directive `.. contents::` but it provided only 2 links: to the title of document and to "Indices and tables" section at the end, but not to my sections.

sphinx 1.3.1.
