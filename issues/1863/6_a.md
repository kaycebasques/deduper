If I'm understanding correctly, this was closed as a duplicate of https://github.com/sphinx-doc/sphinx/issues/2589.

Neither issue seems to be exactly what we're experiencing but it's close, so I wanted to make sure this is captured somewhere.

Effectively, when we do a dirhtml build we get a nice sidebar like:
```
- A
  - A.1
  - A.2
- B
  - B.1
  - B.2
```
(with e.g., the subitems like A.1 and A.2 only visible if you're on page A)

however using the singlehtml build we get a not as nice sidebar like:
```
- A
- B
```

It would be much better if that sidebar was either fully elaborated or elaborated based on where the browser is scrolled in the document (using some JavaScript).