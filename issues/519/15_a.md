Memo:
The `param` block is parsed as field lists by docutils (reST parser). At that time, the parser recognizes trailing underscore as a hyperlink reference.
So, surely this is hard to fix from sphinx side as Georg said.

I'd try to fix this from sphinx side, but we need to escape underscores explicitly at present.