I added a workaround for this issue to Pallets-Sphinx-Themes. A hook will detect if the dirhtml builder is in use and if the `pageurl` value ends with `.html` and replace it with the correct URL. https://github.com/pallets/pallets-sphinx-themes/pull/53