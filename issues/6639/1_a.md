**Describe the bug**
Using routine 

```python
    pofile.write_po(f, catalog, width=0)
```
and there appear to be no ways to control the page width so to retain the original layout. Set to negative or 0 always resulted to 76, and lines greater that length WILL BE wrapped, leading to git flagging up the file as being CHANGED, when in fact, NOT THING has.

**To Reproduce**

Try to read/write a PO file with comments
(ie. take this po file: https://github.com/hoangduytranuk/blender_manual/blob/master/gui/2.79/po/vi.po)

or take it from this zip file

[279_vi.zip](https://github.com/sphinx-doc/sphinx/files/3491637/279_vi.zip)

then write some code making using of 

```python
    from sphinx_intl import catalog as c
    from babel.messages import pofile
```
and write 

```python
    def dump_po(filename, catalog):
        dirname = os.path.dirname(filename)
        if not os.path.exists(dirname):
            os.makedirs(dirname)

        # Because babel automatically encode strings, file should be open as binary mode.
        with io.open(filename, 'wb') as f:
            pofile.write_po(f, catalog, width=0)
```
```python
    from_po_cat = c.load_po(from_vipo_path)
    dump_po(to_vipo_path, from_po_cat)
```
then using something like 'meld' (ie. sudo apt update; sudo apt install -y meld) to compare the input vi.po and the output file.

or simply using:

```bash
    pofile.write_po(f, catalog, width=0)
```


at the command line should not show any changes

**Expected behavior**
With no changes to the messages, the output file SHOULD remain the same, or at least, provide some methods to KEEP THEM the same.

**Your project**
https://github.com/hoangduytranuk/blender_manual

**Screenshots**
![Screenshot from 2019-08-11 12-15-00](https://user-images.githubusercontent.com/43050631/62835084-19544400-bc4c-11e9-9bc4-5fa20ce490f7.png)

![Screenshot from 2019-08-11 06-27-30](https://user-images.githubusercontent.com/43050631/62835075-f9248500-bc4b-11e9-9a7e-08f849250b3a.png)


**Environment info**
- OS: [Linux, Kernel: 4.15.0-20-generic x86_64 bits: 64 compiler: gcc v: 7.3.0 Desktop: Cinnamon 4.0.8 Distro: Linux Mint 19.1 Tessa base: Ubuntu 18.04 bionic]
- Python version: [3.6.7]
- Sphinx version: [1.8.5]
- Extra tools: [meld, diff]



