I want to document several helper scripts in my documentation. All scripts share for example the `-v` and `-d` option to enable verbosity and debug modes. Unfortunately, the index refers only to one `.. option:: -v` directive.

Is there a way to differentiate these options?
Can I define a prefix for the internal name?