Sorry, I figured what was wrong. I need one `.. program::` directive per script, which I'm describing.

Maybe the documentation should be a bit more clear here. The grouping directive (`program`) is explained after the item directive (`option`) and the description for `option` contains no hint to the `program` directive.

This brings me to another problem with `program`. My current ReST looks like this:

```
.. program: compile-xilinx-vivado.ps1

.. option:: -v

   Enable verbose mode.
```

I need to use the full name, so the index will show the correct entry. But would it be possible to have a shorter label or a display name, which can be used in references. `:option:'compile-xilinx-vivado.ps1 -v'` is quite long.

![image](https://cloud.githubusercontent.com/assets/956109/20352737/aa69aab2-ac18-11e6-8f17-7b106dd69809.png)
