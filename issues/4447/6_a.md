Sorry for late.

>Provides an explicit title of An explicit title to foo.rst so should not trigger the warning?

Yes and No. The explicit title is only used to the toctree, not the content of `foo.html`.
Anyway, the warning would not be needed for the case.