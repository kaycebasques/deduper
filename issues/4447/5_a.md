If you are generating the docs from python source code using `sphinx-apidoc --no-headings --no-toc` then the proper way to deal with the warning would be to set a title in each python module for which you are getting the warning, i.e. at the top of each `.py` file add something like

```python
"""
The Larch
==============

A module for recognising different types of trees from quite a long way away

"""
```

