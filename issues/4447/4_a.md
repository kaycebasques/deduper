Right, but according to my understanding, using 

```
.. toctree::
   :maxdepth: 2
   :caption: Contents:

   An explicit title <foo>
```

Provides an explicit title of `An explicit title` to `foo.rst` so should not trigger the warning? 