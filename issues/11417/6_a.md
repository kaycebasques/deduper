As I already wrote:

> I'd like to use the version in the versionadded, versionchanged, and deprecated directives to link to the release notes for that version so a user could see relevant changes that might have occurred in that release. ... I can reference it in the rest of the text, but then it seems like I have to state the version twice.

Expanding on that a bit, the a user could see the PR or PRs that made the change by clicking on the release link, then the PR link in the release notes. This information is in [our release notes](https://opendds.readthedocs.io/en/latest-release/news.html), so I'd like to link to it.

I think the example above was sorta supposed to be an example of the use case, but it's more me just playing with the syntax. Here's a better example:

`releases.rst`:

```rst
.. _v1_0_0:

v1.0.0
------

`Download this release on GitHub <github.com>`__

- Added feature X `(Link to PR #100) <github.com>`__
- Added feature Y `(Link to PR #101) <github.com>`__
```

`X.rst`:

```rst

Feature X
=========

.. versionadded:: :ref:`v1_0_0`
```


  
  

