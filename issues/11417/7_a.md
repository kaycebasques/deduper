A concrete example from `venvstacks`:

```rst
.. versionchanged:: :ref:`0.3.0 <changelog-0.3.0>`
  Added ``win_arm64`` and ``linux_aarch64`` as permitted target platforms.
```

While I *can* put the full release notes link in the change description, there isn't really a great place to do so (I settled for tacking on a ``(:ref:`release details <changelog-0.3.0>`)``, since making the version number itself a link doesn't work).