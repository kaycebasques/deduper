Per the documentation, the first argument is a version number (e.g. `1.2.3a0+dev`).

This renders as `Added/Changed/Removed in version *blah*`. Interpreted text roles can be used after this first argument.

What is the use-case you have to use a role as the first argument? What would you expect it to render? 

A