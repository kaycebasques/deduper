I don't know if I'm the only one, but when showing source code from the documentation I feel like showing the docstring makes no sense (as it is rendered in the documentation). It would be interesting if the `sphinx.ext.viewcode` extension was able to automatically remove them.

That's especially true when the docstring is quite long and the function body quite short.

