Yet another problem with this behaviour is that with non-Latin headers, `id`s are just "section-N", which obviously breaks from simply adding a *sub*section anywhere but at the end of a document.  Labelling sections would be the way to fix that, but...

Also, section labels seem to be ignored by `contents` directive (and likely `toctree` as well), but it's likely *docutils* issue ;(