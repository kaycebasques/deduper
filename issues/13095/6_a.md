Enabling the `autodoc` and `viewcode` extensions, assigning a single module-level `ElementMaker` variable, and then attempting to [`autodata`-document](https://www.sphinx-doc.org/en/master/usage/extensions/autodoc.html#directive-autodata) that variable seems to be enough to replicate this.

The part of the `BuildEnvironment` that seems to be failing to pickle is the `_viewcode_modules` attribute.. and in particular a `modname` that is in fact an `lxml.objectify._ObjectifyElementMakerCaller` object, not a string as the code seems designed for.

That's created/returned somewhere around here: https://github.com/sphinx-doc/sphinx/blob/a1510de4777eaa2e569435f95b05f6f3293d7035/sphinx/ext/viewcode.py#L57-L64

:notebook: For anyone investigating this: the [`--pdb` command-line option](https://www.sphinx-doc.org/en/master/man/sphinx-build.html#cmdoption-sphinx-build-P) for `sphinx-build` is quite useful!