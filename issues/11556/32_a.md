Considering that sphinx could keep track and decide how to determine files are changed, how do we add files to be checked? I have tried to look into the events, and `env-get-outdated` would not do it right? I guess we would have to add that in the `builder-inited` hook, but how do we add sources, and how do we add them such that they are not being parsed by something?

An example would be hooking `sphinx-apidoc` to check for changes in the python files, how would that look like?