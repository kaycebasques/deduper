> Do you mean something like [`env.note_dependency`](https://www.sphinx-doc.org/en/master/extdev/envapi.html#sphinx.environment.BuildEnvironment.note_dependency)?

Documentation wise, it does indeed seem relevant. I will try to search for some references to see if anyone has examples of how they use it. But which hook would we be adding it to? It seems like we will have to add it before `env-get-outdated` to have an effect? But then wouldn't `env` not be available yet?

Edit: Oh, I see, that one is used in directives. For `sphinx-apidoc` example, the rst files to be generated are still lacking, so it should be added before that could be used.