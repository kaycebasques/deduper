At this moment, the python domain does not support nested attributes. It seems the directive considers the prefix of the attribute name as a class name, and strips it if some conditions are satisfied. As a workaround, how about using non-prefixed name like #8657? It might be displayed better than now.