> In this case, it's Python, so the questions is what the supposed meaning of a nested attribute is in the language?

I think a common pattern is a class that has another class as an attribute, which can have its own attributes. Therefore the nested class's attributes are accessible on the parent class by chaining dot notation e.g. `self.env.ref_context`.
In this case, I'm building an API that uses this chained dot notation to "categorise" a number of program options we present to users. For example, if option "c" could be categorised under a category "b", which itself is a sub-category of the root of the option structure, they can access it through `a.b.c`.

> How does it look in the code you are trying to document?

This is done with a simple structure of nested classes. The classes themselves are private and I want the implementation to be opaque to the user, but I want all the attributes to be visible. I don't think autodoc is smart enough for this use case, so at the moment I'm automatically generating this nested tree of `py:attribute::` based on the option structure.

> At this moment, the python domain does not support nested attributes.

Understood, it does seem like a rare use case so that makes sense. Is there anywhere I can read about nesting in Sphinx in general?

> As a workaround, how about using non-prefixed name like #8657?

Definitely possible and thank you for the suggestion - we may end up doing this if the option structure becomes too deep. For now, the workaround I mentioned in the body of the ticket works quite well :)