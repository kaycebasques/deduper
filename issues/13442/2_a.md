I can add it fails on more platforms (tested: `i586`, `x86_64`, `aarch64`, `armv7l`) and on more Pythons (tested 3.11, 3.12, 3.13). So I am quite certain it is a "tame" bug observable everywhere with right Sphinx version.