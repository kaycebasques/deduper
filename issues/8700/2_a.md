I consolidated #8922 into this issue because both have the same underlying cause, which is that autosummary does not inspect docstring metadata when constructing its public template member lists.

The combined scope should cover both directions:

- `:meta private:` excludes an otherwise public member.
- `:meta public:` includes an otherwise private, underscored member. (formally covered in #8922)

A single PR should address both behaviors.