> The linked issue seems to include a workaround + a CSS fix for the workaround but I think we should indeed support what was requested originally. As for _how_ to do it, I have no idea, so PR is welcomed.

I don't think it's really a fix - it relies on `display: none;` to hide the unwanted things. So I think it does merit a real fix, that turns `self` into a more useful and powerful feature.
