Hi!

We could solve it! 

We think the problem is that the .rst was being processed twice, first when processing the parent .rst (with the include) and then the .rst file itself. 

So, we added the included .rst file in the exclude_patterns array and that works fine!

