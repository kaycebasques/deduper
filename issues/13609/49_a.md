> Not all displayed elements are natively "target-able" in LaTeX, for example an equation needs to be numbered, an image needs to have a caption, same for a table. 
...
> However we can at Sphinx render almost anything somewhat easily "target-able" because to start with every paragraph start with `\sphinxAtStartPar` 
...

The Docutils LaTeX writer inserts a `\phantomsection` before the `\label` commands by default and handles elements that don't require this as exceptions.
I  prefer to do complex stuff in the writer, as Python code is easier to program and comprehend than LaTeX code.
