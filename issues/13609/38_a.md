Thanks for testing. Now, we both have the task to fix our LaTeX writers to insert labels for "arbitrary" elements with non-empty `ids`, regardless of a preceding `<target>` element.
The Docutils LaTeX writer (docutils/writers/latex2e/__init__.py) already uses the auxiliary method LaTeXTranslator.ids_to_labels() in Inline Elements and some more. It will be used in all relevant `visit_*()` functions (work in progress, may be too late for Docutils 0.22).