This occurs without the `autoapi` extension, too.  In addition, class properties are overriding the expected reference to the python documentation (through intersphinx).  A small example that uses only the `sphinx.ext.intersphinx` and `sphinx.ext.napoleon` extensions is attached:

[sphinx-bug.zip](https://github.com/user-attachments/files/24619033/sphinx-bug.zip)

In Sphinx 8.3, the reference to `type` correctly pointed to the Python docs, whereas in 9.1 it resolves to the `type` property on an unrelated class.