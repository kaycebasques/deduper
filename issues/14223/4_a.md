Just ran into this with Flask and Werkzeug too. I ignored the warning for now, rather than changing all the annotations to the deprecated form.

```python
# conf.py

# https://github.com/sphinx-doc/sphinx/issues/14223
suppress_warnings = ["ref.python"]
```