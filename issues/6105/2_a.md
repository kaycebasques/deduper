As a perhaps helpful pointer, the C++ domain does the scope tracking using ``env.ref_context`` to make sure the relevant data is present when cross-references are resolved.
Specifically for fields the ``process_field_xref`` method of the domain is used to update tracking (https://github.com/sphinx-doc/sphinx/blob/master/sphinx/domains/cpp.py#L7032).
