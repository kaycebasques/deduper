In my .rst file:

```
  .. figure:: ./images/overview_loc.png
        :alt: Overview
        :width: 50% 
``` 

> `overview_loc.png` is in the same path as .rst file.
> I also added `overview_loc.ja.png` in the same folder.

in `conf.py`, no other image related settings:


```
# -- Localization configuration
locale_dirs = ['locale/']   # path is example but recommended.
gettext_compact = False     # optional.
```

Should I see the magic when I run `make gettext` or at what point, what change happens?