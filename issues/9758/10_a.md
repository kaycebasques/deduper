You mean like - `overview1.jp.jpg`. I tried multiple ways. For eg, put this translated image with the `.jp.png` postfix in `/docs/user/images/overview1.png` or any image folder and my **conf.py** looks like:

```
locale_dirs = ['locale/']   # path is example but recommended.
gettext_compact = False     # optional.

gettext_additional_targets = ['image'] # allows images to be translatable
figure_language_filename = " {root}.{language}{ext}"
```

Here is my image:
`/rr_pa_amr_docs/docs/images/user_doc/sui/Overview/image.png`

Here is my doc path:
`docs/user/system_ui.rst`

What is the right directory to place the image in?