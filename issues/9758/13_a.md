I don't know what does your make file do. You can execute the `sphinx-build -b html -D language=ja . _build/html/ja` command. This will create a folder `_build/html/ja` where you should see the localized screenshot in the HTML output.