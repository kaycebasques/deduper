I’m unable to reproduce this from the provided files. A couple details appear inconsistent:

* The layout places `conf.py` at the workspace root, but the command uses `-c docs`.
* From `docs/index.rst`, `../../other-module/README` does not point to the displayed `workspace/other-module`; that would be `../other-module/README`.

Could you provide the Markdown source with the headings from the failing README files? In particular, do those headings contain a `:download:` role, link, or extension-provided role?
