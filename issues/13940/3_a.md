Ah, that it drops the frame as a fallback makes sense.

Would it be possible to modify code blocks to not have frames, but instead be indented? (As an optional setting.) For me, having code blocks indented without a frame would be preferable anyway, and this would be less problematic in such contexts where the frame causes problems.