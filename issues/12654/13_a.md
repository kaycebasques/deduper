> Being able to test different python version just by adding a `--python {VERSION}` argument to commands is very nice

I would note, I already use [tox-uv](https://github.com/tox-dev/tox-uv) for developing this repo, so this is already easily possible, and you can run against multiple python version in one go, which `uv` does not yet have