> Is there anything currently preventing a contributor from using `uv` to develop Sphinx?

Not really. You could end up with a different version of dependencies in your environment. Committing the UV lock file would solve that.

In the same way, there's nothing stopping someone using pip even if the docs provide instructions for UV