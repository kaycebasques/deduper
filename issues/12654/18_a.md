i'll respond to those in turn

> Does it make sense to commit a lock file for Sphinx as a library?

yes, even libraries should commit lockfiles, because lockfiles ensure a consistent environment between different contributors and CI.

> If so, could we use `pylock.toml`?

maybe? it's a first attempt at an official lockfile spec, and it's missing some of the features that UV provides. I'd be inclined to adopt a wait and see approach for that spec. UV can generate both that spec or requirements.txt on demand if interop is needed.
