> I'm considering merging the `sphinxcontrib-*` first-party packages back into this repo, I find the multi-repo setup annoying to work with locally.
> 
> [@danieleades](https://github.com/danieleades) I see `uv` has a 'workspace' feature, are you familiar with it? Would it make sense with this use-case?
> 
> A

yeah i think that would make sense.

it might cause some initial headaches, since the version resolution is done globally across all packages in the workspace. Since these are meant to be used together anyway this is desirable, but some initial dep hell might occur.

I've not actually used uv's workspace, but i make heavy use of the rust workspace feature that it's based on.

@chrisjsewell has some experience with python workspaces, i believe?