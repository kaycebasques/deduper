I'm considering merging the `sphinxcontrib-*` first-party packages back into this repo, I find the multi-repo setup annoying to work with locally.

@danieleades I see `uv` has a 'workspace' feature, are you familiar with it? Would it make sense with this use-case?

A