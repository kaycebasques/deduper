I agree with @gmilde and think this is a LaTeX limitation a more robust syntax can be chosen through the use of labels, including `top`.  Given that this seems like more of a syntax workaround, I vote to close.