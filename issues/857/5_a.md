I salvaged attached file from bitbucket.org
```
--- /usr/local/lib/python2.7/dist-packages/Sphinx-1.1.2-py2.7.egg/sphinx/ext/autodoc.py 2012-01-18 16:28:08.428919101 +0100
+++ autodoc.py  2012-01-18 16:27:25.776920654 +0100
@@ -257,8 +257,9 @@
         self.object_name = None
         # the parent/owner of the object to document
         self.parent = None
-        # the module analyzer to get at attribute docs, or None
+        # the module analyzers to get at attribute docs, or None
         self.analyzer = None
+        self.local_analyzer = None

     def add_line(self, line, source, *lineno):
         """Append one line of generated reST to the output."""
@@ -454,10 +455,17 @@
                 filename = self.analyzer.srcname
             sourcename = u'%s:docstring of %s' % (filename, self.fullname)

-            attr_docs = self.analyzer.find_attr_docs()
             if self.objpath:
                 key = ('.'.join(self.objpath[:-1]), self.objpath[-1])
+                attr_docs = self.analyzer.find_attr_docs()
+                docstrings = None
                 if key in attr_docs:
+                    docstrings = [attr_docs[key]]
+                elif self.local_analyzer:
+                    attr_docs = self.local_analyzer.find_attr_docs()
+                    if key in attr_docs:
+                        docstrings = [attr_docs[key]]
+                if docstrings is not None:
                     no_docstring = True
                     docstrings = [attr_docs[key]]
                     for i, line in enumerate(self.process_doc(docstrings)):
@@ -688,7 +696,7 @@
         # functions and classes of internal submodules.
         self.real_modname = real_modname or self.get_real_modname()

-        # try to also get a source code analyzer for attribute docs
+        # try to also get a source code analyzer for attribute docs (real module)
         try:
             self.analyzer = ModuleAnalyzer.for_module(self.real_modname)
             # parse right now, to get PycodeErrors on parsing (results will
@@ -703,6 +711,14 @@
         else:
             self.directive.filename_set.add(self.analyzer.srcname)

+        # try to also get a source code analyzer for attribute docs (local module)
+        if self.modname != self.real_modname:
+            try:
+                self.local_analyzer = ModuleAnalyzer.for_module(self.modname)
+                self.local_analyzer.find_attr_docs()
+            except PycodeError:
+                self.local_analyzer = None
+
         # check __module__ of object (for members not given explicitly)
         if check_module:
             if not self.check_module():
```