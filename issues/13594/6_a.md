@AA-Turner I'm just coming back round to this. Based on my above conclusions, I've figured out that we can basically achieve what we want with:
```
.. literalinclude:: code/client_middleware_cookbook.py
   :pyobject: token_refresh_preemptively_example
   :lines: 2-
   :dedent:
```
As long as we ensure the function definition always remains one line. (We could maybe try something like `:start-after: ) -> None:` too).

So, if you don't think it's worth creating a dedicated option for this, I'm happy to close this and proceed with this approach.