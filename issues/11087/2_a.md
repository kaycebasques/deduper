I would also add that it's unclear how section hierarchy is handled between documents. For example, if I use = and -- in one document, and then use = and ^^ in another one, will ^ and -- be of the same level, or of different ones?