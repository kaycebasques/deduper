Would be useful for our team as well. We tried using comment markers like the @andreacassioli to implement what Asciidoctor calls [tagged regions](https://docs.asciidoctor.org/asciidoc/latest/directives/include-tagged-regions/). Had the same disadvantage that we can't strip them out when, for example, including the entire file.