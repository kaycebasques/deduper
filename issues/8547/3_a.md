Hello, I created minimal reproducible example and afterwards I found this issue. Still I add this [example here](https://github.com/Malachov/sphinx_bug_description)

Where it can be the issue I think is the `__init__.py` where I sometimes separate public API with internal code (imported directly from modules, not subpackages)

Functions works just great after import (probably because of `__doc__` with the content in function itself).

Some printscreen from example

This is in subpackage `__init__.py`
![image](https://user-images.githubusercontent.com/39381691/172467164-42fe9ea9-e196-4a8c-8db0-8f6796014cf9.png)

This is in internal subpackage module, from where it's imported

![image](https://user-images.githubusercontent.com/39381691/172467236-675fd805-7bd0-4fcd-b494-3bd15146d011.png)

Here it's OK except the TypeAlias from typing, e.g. 

```python
"""Docs of used Union"""
MyType = int | float
```

But I've also found running issue for this.

One solution is outside the Sphinx in python itself to be able to change the `__doc__` for variable with prepend comment. Another may be in sphinx to go to source and then parse the same way as in file itself maybe...?
