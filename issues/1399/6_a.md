An additional case where you might have multiple glossaries with overloaded/conflicting terms: intersphinx.

I would like to have intersphinx for linking to e.g. API docs for multiple dependencies and would also like to use `:term:` to refer to a glossary definition in one of them that happens to also be present in the other. A workaround appears to be defining the term in my own glossary which then links directly to the desired external term anchor URL, but it's unclear if that precedence is explicit or an implementation detail.