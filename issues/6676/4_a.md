> like pre if a title has text wrapped in code

The approach I took here (for now) was to just take a text version (so loosing the code formatting, keeping the text). But I suppose that in principle it should be able to get the html rendered version of *just* the title (but would need to dive further into sphinx to find that out)

> You can also get at least one p if :caption: is used

When does that end up in a toc item?
