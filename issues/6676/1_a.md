**Is your feature request related to a problem? Please describe.**
I need to heavily modify the toc and toctree in my theme.  I have resorted to manually deprendering them using a template function into a List[dict], and then using standard jinja2 template to render the content that I want.  

**Describe the solution you'd like**
Variables available in the templating enging that express the contents of both menus.

**Describe alternatives you've considered**
I tried to dig into the core of Sphinx to determine how to override these functions but I couldn't find a clean entry point. 

**Additional context**
This is what I'm using now:

```python

def ul_to_list(node: bs4.element.Tag, fix_root: bool) -> List[dict]:
    out = []
    for child in node.find_all("li", recursive=False):
        if callable(child.isspace) and child.isspace():
            continue
        formatted = {}
        if child.a is not None:
            formatted["href"] = child.a["href"]
            formatted["contents"] = "".join(map(str, child.a.contents))
            if fix_root and formatted["href"] == "#" and child.a.contents:
                # TODO: Replace with internal sphinx method to slugify
                formatted["href"] = "#" + slugify(walk_contents(child.a))
            formatted["current"] = "current" in child.a.get("class", [])
        if child.ul is not None:
            formatted["children"] = ul_to_list(child.ul, fix_root)
        else:
            formatted["children"] = []
        out.append(formatted)
    return out


def derender_toc(toc_text, fix_root=True) -> List[dict]:
    try:
        toc = BeautifulSoup(toc_text, features="html.parser")
        nodes = []
        for child in toc.children:
            if callable(child.isspace) and child.isspace():
                continue
            if child.name == "ul":
                nodes.extend(ul_to_list(child, fix_root))
            else:
                raise NotImplemented("Not sure what to do here, expecting only ul")
        return nodes
    except Exception as exc:
        logger = logging.getLogger(__name__)
        logger.warning(
            "Failed to process toctree_text\n" + str(exc) + "\n" + str(toc_text)
        )
        return toc_text
```

- Theme is deployed to https://statsmodels.org/devel; not that there is must to see. 

