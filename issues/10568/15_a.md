> it seems using PEP 695 syntax in the output RST works fine because presumably core Sphinx is not relying on Python's own handling of that syntax.

Yes, it's a different mechanism that I'd like to update once we are using Python 3.12 (but for now we still require Python 3.11).

> The above code works fine on older Python versions with no version test needed

Yes, but I don't know if we need more things to make everything work. Maybe not but since I'm too tired to check I'll leave to the tomorrow-me the task to check. Thanks for investigating it though!