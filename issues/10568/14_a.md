Thanks for your very fast reply (it is only 01:30 here 😃). The above code works fine on older Python versions with no version test needed. `getattr(self.object, "__type_params__", ())` simply returns the empty tuple in this case, and it seems using PEP 695 syntax in the output RST works fine because presumably core Sphinx is not relying on Python's own handling of that syntax.