Hi @jgrey4296 — #14372 un-deprecates Parser.config and Parser.env so they remain the supported way to read config/env from a custom parser. set_application itself stays deprecated, but for your sphinx_bib_domain use case the migration should just be dropping the set_application override and reading self.config / self.env directly inside parse(). Let me know if that works for you.  