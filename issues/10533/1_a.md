### Describe the bug

Hi folks! I'm trying to document my code with Sphinx (readthedocs theme).
But when I have some ABC abstract properties, line breaks disappears when they have short descriptions.

### How to Reproduce

Here's my code to reproduce this bug:

```
from abc import ABC, abstractmethod
from typing import List, Dict, Optional, Union, Iterable, Any
from enum import Enum, auto
from datetime import date

from pandas import DataFrame as PandasDataFrame
from pyspark.sql import DataFrame as SparkDataFrame
from pyarrow import Table


class FeaturePreset:
    """A pre-filled list of feautres for specific model"""
    def __init__(self, name: str, feature_names: Iterable[str]):
        self.name = name
        self.feature_names = list(feature_names)


class BaseDatamart(ABC):
    """A base abstract class for datamarts
    """
    @property
    @abstractmethod
    def feature_names(self) -> List[str]:
        """feature names (column names)"""
        ...

    @property
    @abstractmethod
    def filter_keys(self) -> List[str]:
        """If our datamart has some primary key (client_pin, client_inn и etc) this property returns PK col names
        """
        ...

    @property
    def feature_presets(self) -> Dict[str, FeaturePreset]:
        """Feature presets registered for this datamart (if any)"""
        return {}

    @property
    @abstractmethod
    def dm_type(self) -> str:
        ...

    @property
    @abstractmethod
    def url(self) -> str:
        ...

```

### Expected behavior

_No response_

### Your project

fs_etl

### Screenshots

![image](https://user-images.githubusercontent.com/5650922/172274321-2a5e0265-72e2-4e25-9dee-5d422dbbbd13.png)


### OS

Ubuntu 22.04

### Python version

3.7

### Sphinx version

4.5.0

### Sphinx extensions

_No response_

### Extra tools

_No response_

### Additional context

My dependencies from setup.cfg:

```
    pip >= 20
    pyspark == 3.1.1
    apache-airflow == 2.2.2
    virtualenv
    dill == 0.3.4
    jinja2
    sqlalchemy == 1.4.32
    psycopg2-binary >= 2.9
    typing-extensions >= 4.2
    pydantic
    devtools
    cassandra-driver >= 3.2.5
    pandas >= 1.2

    Sphinx >= 4.3.1
    sphinx-rtd-theme >= 1.0.0
    sphinx-autodoc-typehints >= 1.12.0
    sphinx-copybutton
    recommonmark
    sphinx_-mdinclude
    sphinx-pydantic
```



My conf.py:

```
# Configuration file for the Sphinx documentation builder.
#
# This file only contains a selection of the most common options. For a full
# list see the documentation:
# https://www.sphinx-doc.org/en/master/usage/configuration.html

# -- Path setup --------------------------------------------------------------

# If extensions (or modules to document with autodoc) are in another directory,
# add these directories to sys.path here. If the directory is relative to the
# documentation root, use os.path.abspath to make it absolute, like shown here.
#
# import os
# import sys
# sys.path.insert(0, os.path.abspath('.'))


# -- Project information -----------------------------------------------------
from sphinx.locale import _


project = 'fs_etl'
copyright = '2022, ALFABANK'
author = 'ALFABANK'

# The full version, including alpha/beta/rc tags
release = '0.1.1'


# -- General configuration ---------------------------------------------------

# Add any Sphinx extension module names here, as strings. They can be
# extensions coming with Sphinx (named 'sphinx.ext.*') or your custom
# ones.
# noinspection PyInterpreter
extensions = [
    'sphinx_rtd_theme',
    'sphinx.ext.napoleon',
    'sphinx.ext.intersphinx',
    # 'sphinx.ext.autosectionlabel',
    'sphinx.ext.mathjax',
    'sphinx.ext.viewcode',
    'sphinx_autodoc_typehints',
    'sphinx_copybutton',
    # 'recommonmark',
    'sphinx_mdinclude',
    'sphinx-pydantic'
]
# mathjax_path = "https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"

# Add any paths that contain templates here, relative to this directory.
templates_path = ['_templates']

# List of patterns, relative to source directory, that match files and
# directories to ignore when looking for source files.
# This pattern also affects html_static_path and html_extra_path.
exclude_patterns = []


# -- Options for HTML output -------------------------------------------------

# The theme to use for HTML and HTML Help pages.  See the documentation for
# a list of builtin themes.
#
html_theme = 'sphinx_rtd_theme'



# Add any paths that contain custom static files (such as style sheets) here,
# relative to this directory. They are copied after the builtin static files,
# so a file named "default.css" will overwrite the builtin "default.css".
html_static_path = ['_static']


add_module_names = False  # don't show full module names in index
add_function_parentheses = False


html_theme_options = {
    'navigation_depth': 5,
    'includehidden': True,
}

# Extensions to theme docs
def setup(app):
    from sphinx.domains.python import PyField
    from sphinx.util.docfields import Field

    app.add_object_type(
        'confval',
        'confval',
        objname='configuration value',
        indextemplate='pair: %s; configuration value',
        doc_field_types=[
            PyField(
                'type',
                label=_('Type'),
                has_arg=False,
                names=('type',),
                bodyrolename='class'
            ),
            Field(
                'default',
                label=_('Default'),
                has_arg=False,
                names=('default',),
            ),
        ]
    )


always_document_param_types = True  # add type info for undocumented parameters (RTD theme features).
# exclude_patterns = ["*generate_docs.rst"]
```