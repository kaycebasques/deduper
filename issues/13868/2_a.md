I was able to mess around a little with the internals and get `automodule` to recognize nanobind functions, but it is not pretty:
```python
class FunctionDocumenter(Documenter):
    """Specialized Documenter subclass for functions."""

    props: _FunctionDefProperties

    __docstring_signature__ = True

    objtype = 'function'
    member_order = 30

    @classmethod
    def can_document_member(
        cls: type[Documenter], member: Any, membername: str, isattr: bool, parent: Any
    ) -> bool:

        if '__nb_signature__' in list(dir(member)):
            sig = member.__nb_signature__[0][0]
            if sig.startswith("def"): return True
        # supports functions, builtins and bound methods exported at the module level
        return (
            inspect.isfunction(member)
            or inspect.isbuiltin(member)
            or (inspect.isroutine(member) and isinstance(parent, ModuleDocumenter))
        )
```
This fails for discovering classes, of course, and poses a whole different set of issues. The classes get filtered out before getting handed to `ClassDocumenter` by `_member_finder.py:_filter_members` due to `inspect.getddoc` failing. Perhaps there are better ways to handle discovery? This was just the naive approach... 

Is this something better suited for an extension, instead? I realize that handling edge cases for specific native bindings is not ideal...