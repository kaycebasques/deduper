@tk0miya I don't the sphinx internal so maybe what I'm suggesting is not possible.

My assumption is that if a section has an explicit label added by the user then that label is already  present in the `app.env.domaindata['std']['labels']` dictionary at the time `autosectionlabel` parses that section. Please correct me if I'm wrong.

If this is the case we can do something like
```python
def register_sections_as_label(app: Sphinx, document: Node) -> None:
    labels = app.env.domaindata['std']['labels']
    anonlabels = app.env.domaindata['std']['anonlabels']

    # ___ new code ___
    sectnames = [sectname for _, _, sectname in labels.values()]

    for node in document.traverse(nodes.section):
        # omitted code, here we get name, docname, labelid, sectname

       # ___ new code ___
        if name in sectnames:
            # here we ignore the section because the user has set an explicit label for it
            continue
 
        if name in labels:
            logger.warning(__('duplicate label %s, other instance in %s'),
                           name, app.env.doc2path(labels[name][0]),
                           location=node, type='autosectionlabel', subtype=docname)

        anonlabels[name] = docname, labelid
        labels[name] = docname, labelid, sectname

       # ___ new code ___
        sectnames.append(sectname)
```