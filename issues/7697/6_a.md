@albertored I don't have a good idea to check the label is already defined or not in parallel mode. I'd not like to add an option only available only on non-parallel mode. I think the built document should be the same in both modes.