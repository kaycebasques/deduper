Yes you're right, I haven't thought about parallel builds.
Maybe we can simply check if a label is already defined for that section and in that case avoid adding the autogenerated one. In this way the user can bypass the autosectionlabel extension on certain sections by putting his own label.