@stsewd  Do you mean generating the label "Hello Wolrd-label-2" from a section titled "Hello World", right? If so,  it can't refer by ```:ref:`Hello World` ```, I think. In addition, we need to consider the way to make the sequence number unique in parallel mode.