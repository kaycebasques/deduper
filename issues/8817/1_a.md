xref pytorch/pytorch#51256

Does the `searchtools.js` de-duplicate results? It seems to be searching for `data`, then `parallel` with no filtering. This is with sphinx 2.4.4, but I don't see any relevant changes to `searchtools.js` on HEAD.