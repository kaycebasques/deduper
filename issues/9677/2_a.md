The python doc recommends using the same name to the setter method as the original getter method.

>This code is exactly equivalent to the first example. Be sure to give the additional functions the same name as the original property (x in this case.)
>https://docs.python.org/3/library/functions.html#property

In your example, `obj.username` is a getter of the username, but it does not work as setter. BTW, `obj.set_username` works as both setter and getter. That's strange, but the generated document describes the implementation correctly, I think.