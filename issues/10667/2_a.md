I think that's actually good behavior.

Because the alternative would be to massively reduce the width of the following code block, which would lead to line breaks or scrollbars, either one making reading the code harder.

In this example this wouldn't be that bad, but I think we should assume that code blocks are filled with code lines that are up to 79 characters wide.

That said, the example of course looks bad, but that's something that should be fixed by changing the content itself.