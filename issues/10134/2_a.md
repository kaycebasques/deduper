I think we have to wait for the discussion in https://github.com/numpy/numpydoc/issues/356 because the name of return value without type (ex. `return1 :` in test_one_return) is still not valid at this moment. I'd not like to add a Sphinx custom syntax.