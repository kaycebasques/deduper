Good point. Since #5191 the internals for handling overloads is much better so I think it should be possible to relax this again, though with some non-trivial changes.
Note that without the #5191 fix, your example actually works. The fix protects against if you suddenly have a reference to ``length``, say you wrote ``Complexity:  Θ(:cpp:expr:`length`)``. That shouldn't crash now, but I think it would without the fix.