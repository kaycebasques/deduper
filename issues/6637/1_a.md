# Issue?
I was fooled by the naming convention `fignumber` that I thought only related to figures. I lately discovered that `fignumber` applies to equations, tables, figures and listings. The terminology is therefore a bit confusing. 

# Solution?
I propose to rename the `fignumber` into something else like `itemnumber` or anything better you guys propose.