Unfortunately, it appears that we still generate the content and only *after* do we check whether to include or not. I don't know why this was done like that.

The issue is not limited to `glob` or anything. Just be a random <code>:ref:\`whatever\`</code> and it will still be a warning.