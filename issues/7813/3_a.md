Unfortunately, it's a restriction of autodoc and autosummary. In docutils, transitions are prohibited inside the directives. For example, it can't be used inside `container` directive:

```
.. container::

   blah blah blah

   --------------

   blah blah blah
```

But, IMO, it would be better to remove such a restriction if possible. @shimizukawa What do you think?