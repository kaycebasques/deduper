As @webknjaz commented, the latest Sphinx goes to build a hyperlink to the class document of the type annotation. In this case, it searches for the document of `importlib_metadata._meta._T`, `importlib_metadata.EntryPoints`, and 
 `importlib_metadata.SelectableGroups`. But they are not found in your document.

In addition to this, it seems you have enabled `nitpicky` mode. In that mode, Sphinx emits a warning if failed to build a hyperlink. This is the reason why your document build is broken.

How about adding these classes into your document? I think it will resolve your build again.