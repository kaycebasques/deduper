How does Sphinx resolve Python stdlib types? This is still an issue with referencing `importlib.metadata.PackageMetadata` even after https://github.com/python/cpython/issues/88376.  This is the ~~error~~ warning we're seeing in [pypa/build](https://github.com/pypa/build), with `sphinx-autodoc-typehints` attempting to link `PackageMatadata`:

```
docstring of build.util.project_wheel_metadata:14:py:class reference target not found: importlib.metadata._meta.PackageMetadata
```