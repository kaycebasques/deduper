> Why do the presence of type annotations on these functions cause Sphinx builds to fail?

*I think* the annotations generated like that are equivalent of `:class:` in plain RST so Sphinx attempts to resolve them in order to add respective hyperlinks. If the warnings are ignored, they are rendered as plain text.