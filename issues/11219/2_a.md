i'm not sure how trivial it is to change this behaviour. Looks like visitor methods are generated with

```python
'visit_' + node.__name__
```

so to use `__qualname___` (which can contain periods) you'd have to do some sanitisation.

maybe something as simple as

```python
cls.__qualname__.replace(".", "_")
```