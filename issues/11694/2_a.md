Could we do something like `.. versionXXX: v1:v2:v3 text`?

Or `.. versionXXX: v1; v2; v3` (the separator being `; ` with a trailing space).

Btw, how should it be rendered? should it be rendered as "Changed in v1, v2, and v3" then? 

Otherwise, we could add another option as you said.