> Is this tutorial still accurate with the new autodoc implementation? https://www.sphinx-doc.org/en/master/development/tutorials/autodoc_ext.html

I suspect it is not; I had followed it to create a custom `ClassDocumenter` subclass but it is pulling from the `_legacy_class_based` submodule. Some of the custom behavior only works in Sphinx 9.0+ with `autodoc_use_legacy_class_based`. With the legacy implementation, I was able to manipulate the `self.analyzer.attr_doc` dictionary to handle some attribute documentation rendering.

If there was explicit interface to provide the data structure of attributes (with name, docstring, etc) to include in the rendered documentation, I could easily implement what I need to do. 

Is there a timeline for documentation for this new implementation? 

EDIT: after digging, it would be nice to be able to provide hooks into the set of functions in [`_member_finder.py`](https://github.com/sphinx-doc/sphinx/blob/master/sphinx/ext/autodoc/_dynamic/_member_finder.py) -- I would like to customize the members and `attr_doc` used in [`_generate._document_members`](https://github.com/sphinx-doc/sphinx/blob/master/sphinx/ext/autodoc/_generate.py#L246). In the legacy class-based approach, I was able to subclass the `ClassDocumenter` and I could provide only the `add_content` and `get_object_members` methods (which themselves could leverage `super()`.) Without classes, the autodoc module could use more callback registration, like the `add_autodoc_attrgetter` to reduce the amount of code duplication required to customize behavior.

EDIT2: [This](https://github.com/nasa/condor/blob/py314-annot/src/condor/sphinx.py#L102) is a subclass of the `ClassDocumenter` that allows us to minimally produce the desired API documentation by customizing the `analyzer.attr_docs` and `analyzer.tagorder`. It would be great if the new implementation made it easier to customize this behavior rather than harder.