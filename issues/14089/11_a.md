> A quick note: you can set the legacy class config variable as an extension maintainer.

For the record, I did it with adding this line in my `setup()`:
```python
app.config.autodoc_use_legacy_class_based = True
```

I hope it's the intended way. Also I hope it won't cause issues to my users down the line (I'll make sure to document it).
