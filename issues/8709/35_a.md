> Getting a new refname/ID framework right is a quite complex issue, so I propose to use the [Docutils EP framework](https://docutils.sourceforge.io/docs/eps/) for discussion and planning.

@gmilde just writing an article describing how the existing ID framework works, what is it goals, modularity/API, gotchas - would be a big deal, solving 80% of mental fog around this issue. What is refname and what is ID for the start.

Then, if Sphinx IDs are global and hierarchical, the HTML5 anchors are local to the page. Maintaining mapping of local anchor IDs to global ones may be the solution. If we have the map, we don't need to bother with the conversion rules. Moreover, the function that does the mapping could be overridden. It can even produce non-unique outputs, because there might be no requirement to go back from HTML5 anchor back to Sphinx ID. But the most benefit is that the ID assignment pipeline and process are crystal clear.