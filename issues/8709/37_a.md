> _Identifiers_ are used to identify fragments of the output document and in cross-links to them from other parts of the output document,

What about identifiers for source fragments?

We are dealing here with information loss when going from source NodeID to HTML markup anchor. I don't see that Docutils describes the tree of NodeIDs. NodeIDs should be address in the tree. Then it should be possible to dump the tree of NodeIDs. Then it should be possible to access all related NodeIDs in the hierarchy when generating certain element in output format. I don't see that spec covers it.



