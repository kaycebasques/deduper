> Do you mean something similar to an element's "xpath"?

Yes.By NodeID  I think about XPath like "address".

The resulting HTML5 generator can then use the "address" and any associated fields for it to render content. And if is it a component based generator - like one responsible for HTML5 anchors - it could maintain own state and mapping how to transform "address" + fields into "anchor". Then we could just patch the component and cover it with tests.