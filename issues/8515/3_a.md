My issue is that this is confusing behavior. How should the user know that they shouldn't use the _.rst_ extension when `.. include::`ing a file? Why do they even need to think about this?

Sphinx simply shouldn't output a warning even if they included files have a _.rst_ extension.