This also affects uppercase HTTP schemes (HTTP://, HTTPS://) — since uri.startswith(('http:', 'https:')) is case-sensitive, a valid
  HTTP://example.com link bypasses network checking entirely and falls through to the local-file path, where it is reported as broken.