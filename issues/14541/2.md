Additionally, URIs with a single slash after the scheme (e.g. `ftp:/archive.zip`) also fail to match the regex `([a-z]+:)?//`, since
  it requires `//`. This is a valid URI per RFC 3986 but gets reported as `[broken]`.