> Additionally, URIs with a single slash after the scheme (e.g. `ftp:/archive.zip`) also fail to match the regex `([a-z]+:)?//`, since it requires `//`. This is a valid URI per RFC 3986 but gets reported as `[broken]`.

Please could you provide a current/up-to-date RFC reference that indicates that a single slash after the scheme part is valid?  To my knowledge, two slashes are required in valid URIs.