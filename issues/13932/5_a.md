Would the motivating example be addressed by an include directive? We do that eg in this repo's docs.

I am hesitant to declare support for symlinks as they're tricky to work with and vary cross-platform. I don't know how common the practice of symlinking markdown files in git repos is (again, this has bad effects on Windows!).

If we do press ahead with your patch, I would want more explicit tests and docs for symlink support, rather than simply the path normalisation change.

A
