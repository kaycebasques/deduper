@mgeier does `sphinx-last-updated-by-git` require relative paths?

I'd prefer to store absolute paths internally & display relative paths to the user (e.g. in CLI output). This means we can de-duplicate paths etc. Over time, I'm hoping to convert all internal relative paths to absolute.

If this will cause problems, please let me know & I can revert for 9.1 and consider an alternative approach.

A