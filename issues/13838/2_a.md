If I don't alias the `ssl` module, then the link renders for the class, but inconsistently — the link includes the module prefix for the class while it doesn't for the function.

<img width="443" height="273" alt="Image" src="https://github.com/user-attachments/assets/be92fb81-94dc-4a8c-a5a1-0ca47cba9fc1" />

Here's **test_sphinx.py** for this version:

```python
from __future__ import annotations

import ssl


class TestSSL:
    """
    Test SSL.

    :param ssl: SSL context.
    """

    def __init__(self, ssl_context: ssl.SSLContext):
        self.ssl_context = ssl_context


async def test_ssl(ssl_context: ssl.SSLContext):
    """
    Test SSL.

    :param ssl: SSL context.
    """
    return ssl_context
```