Generally speaking, I like the suggested concept. Note it can behave similarly to how `:program:` and `:option:` work. So `cpp:attr-namespace` would define a namespace (`cpp:attr-namespace:: gnu::@x86
`) and then all attribute refs (like `cdecl`) would lead to to `gnu::@x86:cdecl`.

I think adding `cpp:attribute` is a good idea on its own.