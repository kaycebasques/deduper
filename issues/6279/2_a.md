I think following patch will help xelatex + SVG case.
```
diff --git a/sphinx/ext/imgmath.py b/sphinx/ext/imgmath.py
index eb0d35c25..5bff8def3 100644
--- a/sphinx/ext/imgmath.py
+++ b/sphinx/ext/imgmath.py
@@ -134,7 +134,10 @@ def compile_math(latex, builder):

     try:
         subprocess.run(command, stdout=PIPE, stderr=PIPE, cwd=tempdir, check=True)
+        if builder.config.imgmath_latex == 'xelatex':
+            return path.join(tempdir, 'math.xdv')
+        else:
-        return path.join(tempdir, 'math.dvi')
+            return path.join(tempdir, 'math.dvi')
     except OSError:
         logger.warning(__('LaTeX command %r cannot be run (needed for math '
                           'display), check the imgmath_latex setting'),
```

But it does not work well for xelatex + PNG case. I think that does not prevent to apply this patch to the extension. Because only TeX professionals use the settings.