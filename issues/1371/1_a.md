The text in http://sphinx-doc.org/config.html?highlight=modindex_common#confval-modindex_common_prefix says:

```
#!rst

.. confval:: modindex_common_prefix

   A list of prefixes that are ignored for sorting the Python module index
   (e.g., if this is set to ``['foo.']``, then ``foo.bar`` is shown under ``B``,
   not ``F``). This can be handy if you document a project that consists of a
   single package.  Works only for the HTML builder currently.  Default is
   ``[]``.
```

I tested it with latexpdf and it worked just fine.

---
- Bitbucket: https://bitbucket.org/birkenfeld/sphinx/issue/1371
- Originally reported by: Jan Ulrich Hasecke
- Originally created at: 2014-02-03T13:31:37.227
