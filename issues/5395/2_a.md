Sphinx expects only single pair of `:param:` and `:type:` is used per a parameter. How about using instead?
```
.. py:function:: foo(bar)

   The docstring

   :param bar: A parameter
   :type bar: str, int or float
```