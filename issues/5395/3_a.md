@tk0miya Thanks for you answer!

I would like to use the more appropriate formatting you suggested, but this is much more complicated on my side because my docstrings are generated dynamically from a complex JSON Schema.

Actually I generate docstrings with Numpy-style and I then use the Sphinx Napoleon module. So at first it looks more like this:

```
Parameters
----------
bar: str
    A parameter
bar: int
   A parameter bis
bar: float
    A parameter ter
```

And what I noticed is that, if I replace `Parameters` with `Other Parameters` for example, issue disappeared!

Indeed, in such case the reST docstring created by Napoleon is something like:
```
:Other Parameters: * **bar** (*str*) -- A parameter
                   * **bar** (*int*) -- A parameter bis
                   * **bar** (*float*) -- A parameter ter
```

So actually it may exist a workaround if I use another section name...

On the other hand, would you accept a Pull Request so this could work with any section, transparently to Napoleon users? Hopefully, it's just a dict somewhere to replace with a list... :)
Even if Sphinx does not expect multiple parameters with the same name, it is already almost working as only types are missing. 