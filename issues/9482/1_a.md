
My specific use case is to have same rst sources build documentation for multiple configurations. Each configuration may have its own product name. What I would like is to have one tag to indicate a configuration, and i can use .. replace:: directive to replace the macro with the correct tag name.

For example:
sphinx-build -t "config1" ...

In the rst source:

.. only:: config1
  
  .. |ProductName| replace :: "Prduct Name 1"

.. only:: config2
  
  .. |ProductName| replace :: "Prduct Name 2"

Here I am only specifying a usecase for "replace" , but I can imagine, the benefits of having many other directives being executed conditionally based on tags.

If there are other ways to accomplish this, any suggestions are welcome as well. But I could not find a easy way for my use case, apart from using meta directive in a way its not designed to be used.

Would be interested in hearing the community's opinion on this request.

Thanks 