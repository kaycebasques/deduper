At present, the "only" and "ifconfig" directives behave as filters. So the contents of these directives are processed (executed) once on parsing. At the latter step, they're filtered conditionally. It means Sphinx does not support what you want.

For your purpose, sphinx-selective-exclude would work fine.
https://pypi.org/project/sphinx-selective-exclude/