I consider #9830 can resolve one of the structural problems. For example, it does not fix the "next page" and "previous page" links. They sometimes point to filtered documents.

To resolve the whole of them, we have to disable the incremental build feature if users use the "only" directive. So we should choose another way if we'll resolve it.