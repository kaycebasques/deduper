> Thanks for your explanation. I have a PR up at #9830, is there interest to fix this "structural problem" for toctree? If yes, I can fix up that PR (now there is a merge conflict).

@ngzhian It looks like #9830 was closed (but wasn't merged)? 

Is there any other way to conditionally include references inside a toctree?

I'd like to be able to do something like this (to conditionally certain pages) but can't figure out how:
```
.. toctree::
   :maxdepth: 2
   :caption: Contents

   FIRST  <first.rst>
   SECOND <second.rst>

   .. only:: internal

      INTERNAL NOTES <secret-stuff.rst>
```

This following does manage to hide the referenced INTERNAL pages from the TOC, but the "INTERNAL SECRETS" heading (caption) still appears even in the non-internal build:
```
.. only:: internal

   .. toctree::
      :maxdepth: 2
      :caption: INTERNAL SECRETS
   
      INTERNAL NOTES <secret-stuff.rst>
```