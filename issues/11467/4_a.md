Concerning the constructor argument, this is not handled by the same documenter. One is the AttributeDocumenter and the other should be the MethodDocumenter / autodoc_preserve_defaults module (not sure which one has the final say for enum values but I'll have a quick look at it next week).

Since the original code contains an "if" statement, for me it appears that it was intended that way at the level of the AttributeDocumenter. I think the formatting for the constructor argument was implemented way after (and maybe by someone else) and they did not think about it (or maybe it's even a dataclass issue?)

I still don't know why the attribute is explicitly documented using the member value rather than the enumeration name, but I would lean towards assuming that the explicit check was there for some reason (hence the feature request).

Anyway, I agree that this discrepancy should either be documented or fixed (or handled differently via some configuration value). If I am not wrong, the one having written most of autodoc is @tk0miya, but I don't whether they are still maintaining it (or recalling the rationale behind).
