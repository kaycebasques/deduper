#8974 (duplicate issue) provides a standalone reproducer: build with the text builder, then the XML builder using the same environment.

The XML output incorrectly retains straight quotes because it reuses a doctree cached without SmartQuotes. This confirms the problem occurs when builder-specific transform results are shared across builders.