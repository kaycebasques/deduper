- The warning is emitted by Sphinx when someone is using a legacy interface. It's however not possible to properly indicate which caller it was (it could be part of a very long frame, and we don't necessarily know where it started).
- It *could* be possible to add more traceback information by using `make PYTHONWARNINGS=error" html` instead. This will turns *Python* warnings into errors (not Sphinx warnings). -W turns logging warnings into errors.