Hi, thanks for taking a look.

I should have been more clear:  the issue is that it fails with an assertion (and a message saying to report it as a bug), rather than a meaningful error. Changing the class name makes this go away -- it was just a copy/paste error.

The document does not make sense because the two `TestClass` definitions are the same (in a C++ sense), though I see now that this does not give an error:

```rst
.. cpp:class:: template<typename T> TestClass

   Some text here.

.. cpp:class:: template<typename U> TestClass

   Some text here.
```

So yeah, I see now how it's related to #2057. Perhaps this issue should really just be a comment in that thread? If so, please close this.