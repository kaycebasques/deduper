Hi,

Thanks for this very nice package. After quite a bit of looking around, I haven't found a way to make subclasses of `ObjectDescription` appear in the table of contents. Is there a way to do this? The closest I could find was wrapping the object returned by the `run` method in `ObjectDescription` into a `section` node, but this requires me to add a `title` node before each object description, which I would like to avoid.

A general mechanism to include additional entries in the table of contents would be most welcome. Apologies if there is such a thing; I haven't found it.

Thanks!
