It seems mprops uses magic to realiaze properties for modules. So there is no proper way to document it. How about using a pseudo type-annotated attribute to build document?
```
eviction_threshold : Union[int, str]
"""
Get or set the eviction threshold in bytes. It can also be set to a string,
whose formatting supports byte(B), kilobyte(KB), megabyte(MB) and
gigabyte(GB) units.
"""
```