Another way to use `mprops` is as follows:

```python
@property
def module_property(mod):
    '''mod is the module that this property was defined in'''

import mprop; mprop.init()
```

So `@property` decorator is used with `mprop.init()` instead of `@mproperty`.  I also tried it but Sphinx showed that `[dtr](https://github.com/MegEngine/MegEngine/blob/release-1.4/imperative/python/megengine/dtr.py)` is not a module / can't find module `dtr` . 

> How about using a pseudo type-annotated attribute to build document?

Thanks for your suggestion and I will have a try. :)