-1 for adding a new option to remove comments. Because there is many kind of "comment syntax" in the world. It is big project to support them all.
+0 for adding a custom event to `literalinclude` directive to customize the behavior of the diretive via extensions if somebody sends us a pull request.