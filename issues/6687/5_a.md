AFAIK, there are two modules for searching.
* `sphinx.search`: indexing module in Python
* `sphinx/themes/basic/static/searchtools.js`: searching module in JS (browser side)

We have to write code for both module. So we have to find the way to strip accepts on JS also.
Thanks,