**Describe the bug**
Searching for string containing accented characters does not work when using non accented characters.

**To Reproduce**
* Create a page with accented character `Bézier curve`
* On the built docs search for `Bezier curve`

**Expected behavior**
Searching for string containing accented characters should work when using non accented characters.

I am not familiar with how the search index is generated but something like this in python could help.

```python
import unidecode
unaccented_string = unidecode.unidecode(accented_string)
```

**Your project**
https://svn.blender.org/svnroot/bf-manual/trunk/blender_docs/

**Environment info**
- OS: Linux
- Python version: 3.7.0
- Sphinx version: 1.8.3
- Sphinx extensions:  None
- Extra tools: None

**Extra Context**

https://developer.blender.org/T69590

