_From Chris Jerdonek on 2012-11-05 20:25:50+00:00_

Supporting the insertion of `:ref:` text in index entries would also be nice, e.g. in cpython, using

```
:ref:`textseq`
```

in an index entry would render as "Text Sequence Type — str" in the actual index.
