I think the option I commented is not only for LaTeX. It would be better if we adopted it to other formats.

Personally, I wrote a similar extension for footnotes on building HTML and EPUB; `sphinxcontrib.getstart_sphinx.footnote_relocator`.
https://pypi.org/project/sphinxcontrib-getstart-sphinx/
I suppose writers prefers to locate them nearly the paragraph, but readers prefers to gather them all on the end of chapters, sections or books.

Anyway, it would be nice to provide the option. +1 for the idea!