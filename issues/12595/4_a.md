This is due to https://github.com/python/cpython/pull/117585. `os.path.relpath(None)` now raises `TypeError` but the code in `osutil.py` only catches `ValueError`. The docutils rst file source path (which is what is being relpath'd) can legitimately be None.