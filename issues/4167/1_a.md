### Problem
I have `add_module_names = False` in my Sphinx config, and I'm creating HTML docs for a Python package using `autosummary` and `autodoc`. I don't want to include the full module name in my class and function signatures. The full module name is properly removed from the text of the signature, but all of the `Permalink to this definition` do include the full module name. The full module name is also appended to autosummary/autodoc page filenames.

### Reproducible project / your project
https://stix2.readthedocs.io/en/latest/api/stix2.utils.html - I thought that the permalink for the `deduplicate()` function on that page would be
`https://stix2.readthedocs.io/en/latest/api/stix2.utils.html#deduplicate`, not 
`https://stix2.readthedocs.io/en/latest/api/stix2.utils.html#stix2.utils.deduplicate`

I also thought the page would be `utils.html` instead of `stix2.utils.html`. Did I just misunderstand or could this feature be expanded?

### Environment info
- Sphinx version: 1.6.4
