This bug seems to still be up.

My understanding is that classes without docstrings, but inheriting a class which itself has a docstring are included.

automodules includes classes when they inherit documented classes, even when not documented themselves.