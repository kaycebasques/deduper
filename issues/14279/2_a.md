One issue I'm having with a minimal reproducer is with `.. py:type:: MyAlias`. When I do that outside of pandas, the module path does not appear, but within pandas it does. I believe I may not have autodoc properly setup. In any case, the issue is with the `.` appearing, so I think this is perhaps not important.