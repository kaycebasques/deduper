> There's only `SphinxTransform`, the difference is how you register it (`app.add_post_transform` vs `app.add_transform`).

Chris meant [`sphinx.transforms.post_transforms.SphinxPostTransform`](https://www.sphinx-doc.org/en/master/extdev/utils.html#sphinx.transforms.post_transforms.SphinxPostTransform) (added ~six years ago in Sphinx 2.1). It handles filtering on builder type using private attributes, but doesn't expose details of the builder/etc.

A 