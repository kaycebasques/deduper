This also affects `SphinxTransform` implementations, i.e. I need a builder for resolving cross-refs in post-transforms. For example, I have [a post-transform that creates references to inherited members](https://github.com/sphinx-contrib/lua-ls/blob/main/sphinx_lua_ls/inherited.py#L138) that weren't documented in the current class.