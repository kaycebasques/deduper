Paying more attention to the outputs, I suspect `sphinx.ext.graphviz` might downgrade `dot` errors to warnings. Compare:
```
dot t.dot -o _ ; echo $?
Error: t.dot: syntax error in line 11 near ';'
1
```
... with:
```
sphinx-build
WARNING: dot code 'digraph smf_flat {\n   node [style=rounded SYNTAX_ERROR=<<<<];\n...
...
...
```
Such a downgrade would make no sense in this case considering the `dot` output is entirely missing.

Following the same logic, maybe `sphinx.ext.graphviz` also downgrades warnings to nothing at all which would "explain" #14362 ?