It would be great if `numref` used "Chapter %s" for top-level references, e.g., Chapter 1,
and "Section %s" for sections within chapters. e.g., Section 1.1.
This could be deduced from "%s" part.

Is there a way to provide smarter handler for "numfig_format['section']"?
For example: 'lambda s: "Section %s" %s if len(s.split('.')) > 1 else "Chapter %s" % s'

I tried a simple class with overloaded __mod__ operator but no luck with Pickler complaining.
I would appreciate if this feature is available.