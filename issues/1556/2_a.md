_From Takayuki Shimizukawa on 2014-09-04 13:18:46+00:00_

I thinks the `autolinkcode` feature should be provided by `sphinx.ext.linkcode`. For now, sphinx.ext.linkcode doesn't have enough information to link to exact line at the target external site page.

As you mentioned, `info` parameter of `linkcode_resolve` function (for sphinx.ext.linkcode) should provide these values:
- extract full (project-relative) file path
- line number
