Hello @shimizukawa !! I'd like to be able to reference directly the line of the source code in `sphinx.ext.linkcode` (linking to the file itself is already very useful, but that would be even better !!)

Is it technically possible to inject this information in the `info` parameter ?