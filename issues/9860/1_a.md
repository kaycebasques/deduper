I'm writing a section of documentation for a given (sub-)module (let's say `a.b.c`). I'd like to auto-document everything within that module including sub-modules, classes, functions, etc. within that section, but without the common `a.b.c` prefix in the generated names. I have tried to use the `py:currentmodule` directive, but that doesn't have the desired effect.

