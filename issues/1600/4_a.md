We can modify the prefix of numfigs with `numfig-prefix` like configuration. But it does not change toctree and section numbers. It's very strange for me.
At this moment, sphinx does not have a mechanism to represent "Appdendix". So we should implement appendix feature at first.
