Yes, `todolist` node is replaced with a list of `todo` nodes on resolving phase. So visitor functions on writer are never called.

+0: wrapping whole of todo list with `<div>` is reasonable
-0: arranging todo label and link sentence is too much. Using extension is better for such case.