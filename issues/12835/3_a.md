This doesn't happen for me using `.. automethod::` with a regular staticmethod that isn't a dunder. What might be happening is that autodoc truncates the 1st argument thinking it's `self` (it also does this for or `cls`).

@dimaqq it would be important to include what [autodoc configurations][1] you set in `conf.py`. In case you use [napoleon its configurations][2] should also be included in the bug report.

[1]: https://www.sphinx-doc.org/en/master/usage/extensions/autodoc.html#configuration
[2]: https://www.sphinx-doc.org/en/master/usage/extensions/napoleon.html#configuration