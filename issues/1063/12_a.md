> You can work around this by adding your constants to `__all__`. This is tedious though.

Did not work for me. I am using `sphinx-build` 3.41 with `conf.py`:

```
...
autodoc_default_options = {
    'members': True,
    'undoc-members': True,
}
```
I included some module-level variables in `__all__`, but only the variables which have `#: comment` were picked up :-/