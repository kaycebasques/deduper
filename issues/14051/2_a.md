In my autodoc implementations, I've ended up creating instances of directives and running them directly, instead of creating intermediate RST string and parsing it. Maybe this would be a better approach for python autodoc as well?