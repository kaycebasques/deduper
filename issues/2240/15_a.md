Just curious, why not consider this feature in the upstream `docutils` rather than `sphinx` ? It'll be nice to have the new options in `image` directives.  
It seems they have been working on embedding images, and embedding svgs in the latest releases.  

But I've proposed an feature request about adding `object` as an option, which is refused.  
https://sourceforge.net/p/docutils/feature-requests/83/