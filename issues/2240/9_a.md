Well, they use an `img` and a `<map>`... instead of clickable svg.

About adding CSS: put the above code snipped in `_static/style.css` and follow http://stackoverflow.com/questions/23462494/how-to-add-custom-css-file
