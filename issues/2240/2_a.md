In my short investigation:

Now, Sphinx uses `<img>` tag for embed SVG images.
It disables hyperlinks in SVG images.
To resolve this, use `<object>` tag instead.
But it causes new issues.
1. Sphinx generates hyperlink to original image like `<a><img /></a>` if image is resized. but after replacing with `<object>`, it does not work correctly.
2. In `.svg` file, The `target` parameter is required at all hyperlinks. (if none, hyperlinks do not work)

And more, I worry about resizing parameter (width and height) for `<object>` tag. I don't know they are always work correctly in any browsers, for any SVG images, and so on (of course, simple case looks fine for me :-)

Please give me some advice.
