The second list item is rendered as following HTML:
```
<li><strong>y</strong> (<em>int</em>) – <p>The other foo.</p>
<p>But with two paragraphs.</p>
</li>
```
The two paragraphs are surrounded by `<p>` tag. As a result, they are folded into next line.

As a workaround, I modified styles for `.field-list p` as `display: inline`. It is rendered like this:

![2017-11-16 0 34 28](https://user-images.githubusercontent.com/748828/32844517-f0b76506-ca65-11e7-90e4-c82937726bca.png)

I don't know which is betetr to fix this from HTML side or CSS side.