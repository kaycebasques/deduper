### Problem
* `:py:attr:attr1` does not link to attr1 in the same docstring.
* `:arg:` has no sphinx domain.

#### Procedure to reproduce the problem
```
curl -O https://github.com/sphinx-doc/sphinx/files/981912/sphinx-feature-request.zip
unzip sphinx-feature-request.zip
cd sphinx-feature-request
make html
open _build/html/index.html
```