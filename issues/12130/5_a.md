> One of the problems it has is that it's totally undocumented, and another is that the fixture names aren't made for use externally. A third-party project would make use of it by injecting app and redefining root_dir — both names are so generic that it's impossible to guess what these are w/o prior knowledge.

I started documenting it somewhere. I can try cherry-picking the documentation only if it could help though