I see there's actually a new cli argument `--exception-on-warning`. This may have been what I was looking for, but I'm not sure yet.  Basically, the ide debugger on 7.4.7 was sending me to a sys.exit call, rather than the point of the code that actually generated the error.