### Problem
- Since python 3.6, class definition order is preserved (see [PEP-520](https://www.python.org/dev/peps/pep-0520/)).
- autodoc should use it by default (instead of alphabet order)
- refs: #4706 