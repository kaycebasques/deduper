Note that ``make clean html`` is purely on the Makefile level, and is the same as ``make clean`` followed by ``make html``, so Sphinx doesn't know it should do a full rebuild. I guess the ``-a`` and ``-E`` options for ``sphinx-build`` are for doing a full read and write? Though I don't know if they make a difference in execution time compared to ``make clean html`` (I expect not).