Hello of course...

I made an example on github [here](https://github.com/Malachov/travis-tests)... It's already builded...
It's only few lines of code. 

If i remove napoleon extension, dict is OK (but no section), If add napoleon, section is highlighted, buit dict is corrupted.

Printscreen from simplified example:

![image](https://user-images.githubusercontent.com/39381691/129084064-d5f0a179-90ab-4d4e-ac12-2f3f72b32684.png)

### Update:
I found, that i can use code directive (two colons) which solve my problem finally, so you can **close the issue** if you want. This is also in example and look like this

![image](https://user-images.githubusercontent.com/39381691/129082874-2b386b01-2f10-41e7-971b-d5f949472228.png)

```       
"""This is docstring docs

Options:
    ::

        {'key': 1, 'key2': 2}
"""
```

When i tried it first, i've forgot empty line (double indent is also necessary, else not highlited)

If it's still an issue for somebody

I don't know whats simplest solution. Adding class to custom section (let them use own CSS) and do not any processing may be sufficient and general enough. Or maybe add next section (next to params and return style) where section heading will be bolded and line break added

