> In the previous example, this would give:
> 
> .. autodata:: pkg.constants::foo
>     :modpath: pkg.foo
> 
> However, this is quite ugly.

Also it would not work if this is done through autosummary and not autodata, see https://github.com/sphinx-doc/sphinx/issues/12020