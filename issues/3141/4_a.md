Sorry. I'd overlooked.
Indeed, you can use `:ivar:` keyword in info field list.
http://www.sphinx-doc.org/en/stable/domains.html#info-field-lists

Anyway, Sphinx does not provide way to represent class and instance attributes distinctly.
(We can also use `:cvar:` keyword in info field list. But it is combined to `:ivar:` keyword and becomes `Variable` field.)
