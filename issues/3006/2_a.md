Note:
`Non Alphabet` is invalid as `id`:

```
ERROR(RSC-005): Error while parsing file 'value of attribute "id" is invalid; must be a string matching the regular expression "[^\s]+"'.
```

so #3000 should be applied to this case.
