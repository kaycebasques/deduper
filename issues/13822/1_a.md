### Describe the bug

I have an GIT Action which should create Sphinx Documentation.
When running "sphinx-apidoc -f -o target source" the process fails at:

sphinx/ext/apidoc/_cli.py line 257 -> command: locale.detlocale(locale.LC_ALL, '')

locale.Error: unsupported locale setting

I also tried to run "export LC_ALL=en_US.UTF-8" upfront but following warning is thrown when trying to do this:
setlocale: LC_ALL: cannot change locale (en_US.UTF-8)

I don't know how to get action working.
I found this issue from past: https://github.com/sphinx-doc/sphinx/issues/5724
But the implemented workaround seems no longer be there in actual version :-(

Thanks in advance,
Hardy


### How to Reproduce

See linked issue - hopefully same. Because working on Win and GIT action based on Ubuntu I have no easy way to check/reproduce :-/
$ python3.10 -m venv venv
$ . venv/bin/activate
(venv) $ pip install sphinx
(venv) $ export LC_ALL=
(venv) $ sphinx-quickstart

### Environment Information

```text
sphinx 8.2.3
```

### Sphinx extensions

```python

```

### Additional context

_No response_