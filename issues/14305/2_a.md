Thanks for the report. Could you provide a minimal reproduction, including the relevant autodoc directive(s), conf.py, and the exact six version?

To help narrow this down, could you also try each of these settings independently and report whether either changes the warning?
```
autodoc_use_type_comments = False
```
```
autodoc_use_legacy_class_based = True
```

The first specifically disables type-comment processing during signature discovery; the second switches back to the legacy autodoc implementation. This will help determine whether the regression is in the new Sphinx 9 autodoc path and, if so, which part of it.