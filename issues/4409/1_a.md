## Config: add_function_parentheses and default_role=any

The config parameter `add_function_parentheses` applies to ``:meth:`Cls.method` `` and ``:meth:`method` ``.

I would have expected that it also applies to `` `Cls.method` ``, `` `func` `` when the the `default_role` is set to "any" or "obj". However, in these cases parentheses are never added, independent of  `add_function_parentheses`.

Is this a bug or is there some reason behind this? 