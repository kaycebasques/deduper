In python domain, `add_function_parentheses` effects only to `:func:` and `:meth:` roles. I don't know this is bug or not. But it is difficult to add parentheses for `:obj:` and `:any:` roles at current implementation.