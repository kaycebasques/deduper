Regardless whether it's a bug or not, I normally write something like `` `os.scandir()` `` when I'm using the `any` role. In other words, I add the parentheses manually.

This way, Sphinx doesn't need to add the parentheses, but more importantly, I also think this is more readable and looks nicer in the source.