I get similar results. I guess it is fair that if you search for `custom event` you also get entries for  `custom` and `event`. It is a matter of ranking: IMHO you should first see those pages that matches both terms, then the other.
