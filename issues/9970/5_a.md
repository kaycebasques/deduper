I don't know how sphinx/autodoc constructs the function headers (probably using `inspect` module?), but theoretically I would expect that it should be possible to get both the short name as written in the code and the absolute path for the hyperlink.