Ah, so the issue is that there currently is no way in rst/sphinx to note the full hyperlink to a short-form default value or type hint, right? Would it be possible to add this functionality to the syntax, or is it out of reach?
Or as a different approach, would it perhaps be possible to adapt the link resolving strategy to also work without the full path? Of course there's a risk of ambiguity, but in the most common situations it might work correctly, given that developers tend to use unique naming for classes and functions.