Ok from grepping through the sphinx source code, this falls into the docutils package.  Indeed, rolling back docutils in macports to py39-docutils @0.17.1_0 fixes this issue.

So py39-docutils @0.18_0 has this bug.