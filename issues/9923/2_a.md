The html generated seems to open a `<div>` tag but close with an `</aside>` tag.  I believe these should match.  Editing the html fixes this.

```
<div class="admonition note">
<p class="admonition-title">Note</p>
<p>(*) The command in this script is a docker command and can be used in Windows.</p>
</aside>  <--previously
</div>    <--add this line
```