In design of Sphinx, extensions can't touch user configuration during `setup()` function. If you want to do it, please use sphinx core-events (ex. `config-inited`, `builder-inited` and so on).
I think this is designed behavior, not a bug.