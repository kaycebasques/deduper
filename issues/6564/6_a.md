>If you tell me how the Python interface for Sphinx latex writer would look like I will look into it.

Simply we can get the specified width from `node['width']` on `visit_table`. As an important notice, it takes multiple types of number: bare number (100), percentage and number having units (ex. `100px`). I think it is hard to support a number having units on LaTeX. So we have to check the value before generating TeX code.

>There may be some issues then with nested tables for examples or in presence of other complications.

I think restrictions are acceptable.