This issue has been fixed since 0.15 version of docutils. However, I am currently the 2.4.4 version of Sphinx with 0.16 version of docutils, the `width` option simply does not work. For example, `:width: 100%` option doesn't add `style="width: 100%"` to the `table` tag in HTML.