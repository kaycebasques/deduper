@tk0miya it is feasible to support width in LaTeX, perhaps imperfectly depending on other constraints on table. Starting point would be to hook into our table templates and into sphinx.sty file (definition of the `\X` and `\Y` column types) in the places where one sees `\linewidth` and prefix this `\linewidth` with some scale. In particular the tabulary table template anyhow expects to be given a total width which is currently `\linewidth`. If you tell me how the Python interface for Sphinx latex writer would look like I will look into it. There may be some issues then with nested tables for examples or in presence of other complications.