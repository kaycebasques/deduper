This one could be done by changing the node being generated, namely you need to remember that the node you are referrencing contains a math equation. Then, when you write your reference node (i.e., the one pointing to the section title), instead of creating ``<span class="std std-ref">{\rm K\alpha}</span>`` you would create something like ``<span class="math std std-ref">\({\rm K\alpha}\)</span>``, so you should add the math class to the reference node and `\(...\)` (namely you duplicate the node that the reference found, except you change some classes).

I'm not sure how to integrate everything and where to patch things, but it's definitely something we can fix.