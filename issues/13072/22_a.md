Hi,

For directives, what would be the way forward? Currently, I use `app.builder.outdir` and `app.builder.name` fetched over `self.state.document.settings.env.app` within https://github.com/SAP/swagger-plugin-for-sphinx. How can I access the builder within a directive if I don't want to pass it over the setup method