Maybe you could emit a custom event each time a `create_thing` directive is encountered, and when creating a `list_things` node, `connect()` (using `functools.partial` to pass the `list_things` instance) a callback so that you can add to the list every time a new _thing_ is created. That way, everything is consistent at the end of the read phase.

Not sure if this would have a big performance impact