Ok, thanks.  Some of the documentation may need updating.

I'll look at the recursion issue soon.  An extremely minimal sample case -- a near-empty `conf.py`, `index.rst`, and any other supporting files required to replicate the problem -- would be _extremely_ useful to figure this out.  If any of the documentation projects that exhibit the problem are open-source-licensed, I'd recommend starting with one of those, preferably a small one, and narrowing down from there -- that's better than creating a minimal example from scratch, because it'll be more likely to replicate the problem accurately.