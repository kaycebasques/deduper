> Todo: if my working so far is correct, then _any_ use of `{% extends "!layout.html" %}` in a project-local theme directory would fail due to infinite recursion, regardless of whether the inherited theme has a `layout.html` file or not; that theory needs to be checked.

Yes, that is my experience