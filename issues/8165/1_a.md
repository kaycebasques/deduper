I'd like to be able to generate documentation for every function, class, and method in a module (from the relevant doc string in the code), with a section containing a message like "missing doc string" for any function, class, or method that doesn't have a doc string.  I understand that Sphinx can't at present do something like this.

I would like to volunteer to work this enhancement, but will need a bit of guidance.  In particular, I'm unclear re. whether this should be a change to Sphinx itself or a Sphinx extension.  If it should be implemented as a Sphinx extension, I'm hoping that someone can direct me to an existing extension that I might use as a jumping-off point.