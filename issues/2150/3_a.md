I'm new to sphinx and the main reason I decided it would work for our project was the existence of the `only` directive.

But now I see that it throws lots of errors because of not excluding the contents of false `only` directives in the first pass. I'm getting many `Duplicate substitution definition name:` errors for example, when to me having a variable substitution is the _point_ of a directive like `only`.  

Very frustrated at this point and trying to see how to work around this strange limitation.
