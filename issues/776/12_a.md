I'm not a search specialist, but I'm willing to dig in.
Currently documenting IP's and other technical data is pointless in sphinx and I'd like to help out to make it not pointless.

Any idea where to look/being? Because I can figure it out if I just get a nudge on where the searchbox in HTML search is created etc.

The base concept appears to be https://github.com/sphinx-doc/sphinx/blob/a1c10f5d5e9734c6722d04b0b0781a5d88860745/sphinx/themes/basic/static/searchtools.js#L237-L244

Which appears to be a combination of simple hay stacks to search from.
The minimal "fix" to be able to search for IP addresses appears to be: https://github.com/sphinx-doc/sphinx/blob/a1c10f5d5e9734c6722d04b0b0781a5d88860745/sphinx/themes/basic/static/searchtools.js#L255-L259

```diff
@ const _escapeRegExp ...
-    string.replace(/[.*+\-?^${}()|[\]\\]/g, "\\$&"); // $& means the whole matched string
+    string
@ query: ...
-        stopwords.indexOf(queryTermLower) !== -1 ||
-        queryTerm.match(/^\d+$/)
+        stopwords.indexOf(queryTermLower) !== -1
```
Which overloads the user with results *(obviously)* if the documentation contains a lot of numbers. This is because `Search._index.terms` contains a rudimentary `split()` on all the text objects found:

```javascript
> Search._index.terms
{0: Array(2), 1: Array(2), 2: 4, 5: Array(2), 6: Array(2), 10: Array(4), 20: 3, 22: Array(2), 66: 1, 100: Array(4), 101: 2, 102: 2, 103: 2, 104: 2, 105: 2, 106: 2, 108: 2, 168: Array(2), 172: Array(2), 192: Array(2), 252: 5, 255: 5, 7670: 4, ip: 0, plan: 0, management: Array(3), client: Array(3), …}
```

Need to find where these terms are populated, as I suspect a more useful index "database" would be helpful, such as not splitting words in quotes and data from CSV or other external sources should be left as-is.. Along with some tweaks to these "regex-escape" and "hide digits" functions.