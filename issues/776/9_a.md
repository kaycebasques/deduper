Update: I had no luck testing the search for the term "3D-Objects" in our examples after installing the current 2.0.0.dev20190213 branch. In spite of the mentioned bugfix in in https://www.sphinx-doc.org/en/master/changes.html#bugs-fixed that reads "HTML search: search always returns nothing when multiple search terms are used and one term is shorter than three characters" is still not fixed. 

"we objects" returns nothing, "wer objects" returns results both for "wer" and "objects".

 I ran a clean build, uploaded the result for testing the search to a webserver and cleared the cache to make sure to use the latest results.