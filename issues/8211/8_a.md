Essentially, it downloads an inventory file and parse it by `InventoryFile` class. I don't think it is not common feature. I'd not like to make it public. And I don't think it does not prevent making 3rd party tools.