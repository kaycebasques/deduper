How about install your custom reader instead? Then you can disable the Substition transform.

>An alternative is to create a Transform that re-adds these substitutions, and apply them in the Writer (just before the Translator), rather than in the Reader.

You can use a post-transform for such purpose. It is called on resolving phase (after cached).
I hope this document helps you understanding Sphinx events and components.
https://www.sphinx-doc.org/en/master/extdev/appapi.html#sphinx-core-events