I think this issue contains 2 topics.

1. `:any:` role sometimes not working as `:class:` doing.
2. `:any:` role (and `:class:` role also) does not support nested types (ex. `list[int]`, `dict[str, int]`).

For the former topic, it's working fine on my local. So we need to reproduce to investigate what is happened on your environment.

On the other hand, for the latter one, it would be better to add such a new role like `:py:type:`.