Not sure if related to this bug or a separate bug. For my dispatch methods, I have the ``@staticmethod`` decorator underneath the ``@singledispatchmethod`` and ``@methodname.register(type)`` decorators. The word _static_ is not appearing next to the method name.