@ARDivekar The issue is equations with multiple lines, but without alignment characters.  In this case, each line is still right-aligned, even though the whole equation is left-aligned as per your `"displayAlign": "left"` suggestion:

```rest
* The following equation is a problem:
  .. math::
    \frac{\partial f}{\partial d} = \frac{\partial f}{\partial e} \cdot \frac{\partial e}{\partial d}\\
    1=1
```

<img width="293" alt="image" src="https://user-images.githubusercontent.com/1028433/134829772-0b007b42-3058-4d24-a803-8d69170c745d.png">

Notice that the second line is right-aligned despite the `"displayAlign": "left"` which makes the whole equation left-aligned.  