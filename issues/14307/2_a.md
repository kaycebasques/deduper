I've thought of two possible exceptions:

- `.test` domains may be configured in downstream test environments -- some documentation projects could conceivable have a test-mode build that mirrors production but uses different URLs?
- Similarly, `.localhost` domains could be used with local webservers that might host documentation URL targets -- again, probably mostly for testing purposes, but I suppose documentation could exist that refers to self-hosted content on well-known ports.