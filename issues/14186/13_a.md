I'm having the same issue. It seems like poetry is deleting the `roman_numerals` package from my `site-packages` directory within my environment. When I delete both `roman_numerals_py-4.1.0.dist-info` and `roman_numerals-4.1.0.dist-info` manually and run `poetry install` again the `roman_numerals` package reappears.

Interestingly, `roman_numerals` doesn't disappear when I downgrade from `4.1.0` to `3.1.0`, however, after updating (both roman-numerals and roman-numerals-py) from `3.1.0` back to `4.1.0` it disappears again.

I could reproduce this issue on both my macOS device and on a GitLab runner running Linux.