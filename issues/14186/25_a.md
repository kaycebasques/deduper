I ran into this with `uv` and `Sphinx 9.1.0`. For some reason `roman_numerals` was listed as installed but not present in the site-packages. My workaround was to `uv pip uninstall roman_numerals` and then `uv sync`. I don't know what happened, but maybe it'll help others to see this comment.