@namurphy if you're on a relatively recent version of `sphinx`: does the [`--quiet` option](https://www.sphinx-doc.org/en/master/man/sphinx-quickstart.html#cmdoption-sphinx-quickstart-q) achieve more-or-less what you want here?

It can be supplied via the makefile like this:

```sh
$ make linkcheck SPHINXOPTS="--quiet"
```

Edit: link fixup