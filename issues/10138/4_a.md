Thank you for example. It's just my guess, but I think the `--separate` option would help you. Could you try it please?
```
$ sphinx-apidoc -f -o ./source/ ../python_dir/python_module_dir/ --separate
Creating file ./source/python_module_dir.rst.
Creating file ./source/python_module_dir.python_functions.rst.
Creating file ./source/modules.rst.
```

Note: I used the following Dockerfile to investigate:
```
FROM python:3.9-slim

RUN apt update; apt install -y build-essential curl git unzip vim
RUN git clone https://github.com/swarajbadhei/sphinx_doc.git
WORKDIR /sphinx_doc
RUN pip install Sphinx
WORKDIR /sphinx_doc/documentation
```