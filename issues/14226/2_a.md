As `°`/`|deg|` is a standalone character, why isn't it defined in `isonum.txt` with `:trim:`?  
Maybe because if trimming is on, it will eat up all spaces, thus it can't be used with space at all. (?)

When I redefine it as
```ReST
.. # Loaded in the prolog file
.. include:: <isonum.txt>

.. # Local definition
.. |deg| unicode:: U+00B0
   :trim:
```

I get this error:
> <rst_prolog>:7: ERROR: Duplicate substitution definition name: "deg". [docutils]

It would be nice if users could redefine replacements, right?  
Of cause it's also good to indicate a possible error my warning of a redefinition.