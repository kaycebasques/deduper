I will try to elaborate the documentation soon. Example 2 and 7 can probably be made working relatively easily, while 4 and 9 is a bit more complicated. I suspect example 3 require substantial code changes as partial specialisations make it complicated (e.g., `template<typename U> SomeClass<std::shared_ptr<U> >`, where the declaration used `T` instead of `U`).
