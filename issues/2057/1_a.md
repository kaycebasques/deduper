Some version info:

```
# Sphinx version: 1.4a0+
# Python version: 2.7.9 (CPython)
# Docutils version: 0.12 release
# Jinja2 version: 2.8
```

So given a templated function:

```
... cpp:function:: template<typename T> void f()
```

The ability to reference it seems to be unavailable. I have tried:

```
:cpp:func:`f`
:cpp:func:`f\<T>`
```

But neither work.

This is also true for anything else I've tried, like:

```
... cpp:class template<typename T> some_class
```

The corresponding (and documented)

```
:cpp:class:`some_class\<T>`
```

does not work.

I am also not sure how to reference functions or classes with variadic templates.
