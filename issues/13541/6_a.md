Options two and three are site specific. Option one is an improvement for site admins. I'd say robots.txt support would be ultimately the best solution here, but the most complex ask.

For option three, I did try using rate-limiting and it made the problem much worse as shown above. I say worse because then each CI job ate more system resources over time at the scale we are seeing. Rate-limiting slowed each connection down to what should be the default as I had requested in the robots.txt file.