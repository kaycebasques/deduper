**Is your feature request related to a problem? Please describe.**
I use `code-block:: cpp` to demonstrate how features in work quite extensively, and it would be beneficial for readers to be able to click on the entities that are mentioned and go to the page that references them (if they're available).

**Describe the solution you'd like**
The example section of cppreference's [`std::array`](https://en.cppreference.com/w/cpp/container/array#Example) page is a good example of what I'd like. Notice that it's possible to click on `std::string`, which takes you through to the `std::string` documentation page.