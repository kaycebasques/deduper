> It is only used when the `-W` (warning-is-error) option given. In such a case, should it inherit `Warning` class?

Purely based on its function? Arguably not.
Based on its name however I would lean more towards "yes", as the latter unfortunately implies that one is dealing with a `Warning` subclass here.