```python
import shutil
from pathlib import Path

from sphinx.cmd.make_mode import run_make_mode

def write(filename, text): Path(filename).write_text(text, encoding="utf-8")

write("test_module.py", '''\
from __future__ import annotations

from os import PathLike
from typing import Literal

MyLiteralAlias = Literal[1, 2, 3]

__all__ = ["foo"]

def foo(path: PathLike[str], literal: MyLiteralAlias) -> None: ...
''')

write("conf.py", '''\
import os, sys
sys.path.insert(0, os.path.abspath(".."))
autodoc_type_aliases = {"PathLike": "os.PathLike"}
extensions = ["sphinx.ext.autodoc"]
nitpicky = True
''')

write("index.rst", '''\
.. autofunction:: test_module.foo
''')

shutil.rmtree("_build", ignore_errors=True)
run_make_mode(["html", ".", "_build", "-T", "-W"])
```

Run as `python reproducer_10525.py`

Alex -- I believe this still demonstrates your problem, failing with 
```
Warning, treated as error:
S:\Development\sphinx\issue_10525\test_module\sphinx_test.py:docstring of test_module.sphinx_test.foo::py:class reference target not found: PathLike
```

If I've simplified the reproducer too much, please let me know.

A