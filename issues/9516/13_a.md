@glp1308 Thanks for info. This is weird indeed as `make latexpdf` (once) triggers  `Latexmk` which determines dynamically how many (pdf)latex passes are needed. And some TOC (perhaps with wrong page numbers) will show on second pass, I don't know how `Latexmk` could decide to make only one latex pass...