> Could it have something to do with the size of my project (over 300 pages) coupled with multiple hundred hyperlinks both internal and external, that all need to get page references printed inline?

I don't see from LaTeX point of view how this could influence the matter... to diagnose this the console output of the Latexmk run would be needed... but the reported script does `make latexpdf > /dev/null 2>&1` so this info gets lost.