However, solution based on `suppress_warnings` implies a `type` and a `subtype`  when `dedent_lines` function in `code.py` calls `logger.warning`.
For the hook solution, I would appreciate very much any tutorial suggestion or existing example.
Once again many thanks for your answers.