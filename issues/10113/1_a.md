Allow disabling on file level, e.g. for the `index.rst` that needs to render on `PyPI` that's not aware of extlinks this shouldn't be enabled (feature added by https://github.com/sphinx-doc/sphinx/pull/9800). cc @hoefling
