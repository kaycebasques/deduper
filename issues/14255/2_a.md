Thank you for the bugreport @zaufi!

NB: some of the styling/formatting of the repro case may be a little messed up -- I had to de-duplicate one of the double-colons, and to add a couple of backticks around the use of the hyperlink when reproing this locally.

I am able to replicate the problem, and from adding some debug `print` statments -- I think this is due to the use of [Anubis](https://github.com/TecharoHQ/anubis) on the website; Sphinx's `linkcheck` builder doesn't evaluate any scripting from the received HTML, so it won't pass the proof-of-work challenge that most human-operated browsers perform by default to reach the destination HTML page with the `changelogs` anchor.