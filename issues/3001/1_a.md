1. re-running `make epub` after removing generated epub doesn't re-generate epub.
2. re-running `make epub` after editing rst generates invalid epub. EPubCheck complains:

```
ERROR(OPF-074): Package resource 'nav.xhtml' is declared in several manifest item.
```
