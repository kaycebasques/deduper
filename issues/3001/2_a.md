To support this, we have to change the structure of builders.
Now packaging EPUB document is invoked after writing HTML files.
But Sphinx exists if no HTML are updated.

We have to invoke such post processes always even if no documents are updated.
I think this is a bug, but the restructure is not stable change. So I postpone this to 1.6 release.
Sorry for inconvenience. Please use `make clean epub` instead.

Thanks,
