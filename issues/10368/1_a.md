**Is your feature request related to a problem? Please describe.**

While working on #10329 , I could not find a clean way to generate domain indices later only because the rellinks would have contained domain names that haven't generated any objects.  In order to not break rellinks, I couldnot make domain indices generated only when they are *really* needed.

This prevent domain indices from leveraging anything that happens after or while `prepare_writing`.

**Describe the solution you'd like**

I'd like rellinks to not rely on domain indices to be generated sooner than they are required.

- Additional class attribute for indices?
- Exclude domain indices from rellinks altogether?