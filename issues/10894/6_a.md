Thanks for the advice. I'm going to try this strategy directly in pelican-myst-reader and see what it gives.

@AA-Turner is there a simple way to get only the content of
```
<div class="body" role="main">
```
in the index.html produced by sphinx-build? I guess this is what I actually need. Should I use a custom builder?

If I need to use bs4 to parse the html code produced by Sphinx for each piece of MyST code, it's going to be very inefficient.
