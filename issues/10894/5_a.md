I meant to re-open when changing the title. I welcome a PR (draft or otherwise) for this, but I don't have time to look into this myself.

Likely your fastest approach would be to work with how Sphinx is currently set up (overwhelmingly in favour of on-disk files)--create a temporary directory containing a single `index.rst` and your `conf.py` that you then run Sphinx on, read, and return the result as a string.

A