@AA-Turner just to note, this was not really about myst, just that in general sphinx doesn't really have a user facing API for running it outside of the CLI (sphinx-build) and/or to parse text that is not a file on the file system, i.e. like `docutils.core.publish_string`.

So therefore also MyST-Parser, or any sphinx parser extension can offer this functionality.
