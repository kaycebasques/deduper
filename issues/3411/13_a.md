> I will handle first the improvement alluded to in a previous comment so that cell contents with multiple paragraphs are better rendered when `tabulary` is used, and cells with lists or other display material will not trigger fall-back to `tabular`, so use cases of `tabulary` are more numerous.

@gmilde Done at #13705.