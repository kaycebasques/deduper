> We can distinguish them by using `table_node['classes'].`
> If user specified the spec, the `colwidths-given` is added into the classes list.

ah thanks, I had wrongly thought `colwidths-given` was always present in case `grid` because default is `auto`, not `grid`.