No, it does not. Appending `.svg` does not help. Instead removing:
-  `.` from version numbers `3.10`, `3.11` and
-  the pipe character `|` (`%7C`) and
- the spaces  (`%20`)

works.. but that's a bad workaround.