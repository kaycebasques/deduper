Sphinx does seem to override the math implementation, so rendering to mathml does not work out of the box. I hacked some mathml output [here](https://github.com/fvkluck/sphinx/tree/example_render_to_mathml), to get a feel for what parts affect the math rendering. This is my first time looking into the internals of sphinx.

I've got some time to work on this today (I'm on a train journey). I'll see if I can come up with a good solution for adding this as an option to sphinx. One option would be a `sphinx.ext.mathml` to support mathml rendering. But since in this case the rendering basically 'falls through' to the docutils rendering, I'll also see if there's a more intuitive way to make that apparent. To be continued :)

