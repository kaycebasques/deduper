> Thanks for the explanation. I believe the two comparable directives are `csv-table` and `raw`. The both allow the content in the body or alternatively inject it via a `:file:` option. The other directives are different in that they are solely designed to work handle external files.

The Docutils "[include](https://docutils.sourceforge.io/docs/ref/rst/directives.html#include)" directive has already options "literal" and "code".
An option "math" could be added to include the file as "math" directive content.

+1"include" offers options to include only a part of the file (e.g. an equation contained in a complete LaTeX file) with the start-line/end-line and start-after/end-before options.

-1 uses the upstream "[math](https://docutils.sourceforge.io/docs/ref/rst/directives.html#math)" directive which is somewhat different from the Sphinx variant.
