Thanks for the explanation. I believe the two comparable directives are `csv-table` and `raw`. The both allow the content in the body or alternatively inject it via a `:file:` option. The other directives are different in that they are solely designed to work handle external files.

So, yes a `:file:` option would be suitable. Do you want to make a PR?

> Also as an option, not an argument [`:path:` is listed in Common Option Value Types](https://docutils.sourceforge.io/docs/ref/rst/directives.html#common-option-value-types).

I read this to be a predefined *type* one can use for options. It's not an option name. So the `:file:` option should be of type *path*.
