Sorry for the late response. Does this still happen? I tried to reproduce the trouble with this Dockerfile. But it seems working fine (no signatures are displayed).

```
FROM python:3.8-slim

RUN apt update; apt install -y build-essential curl git make unzip vim
RUN git clone --recursive https://github.com/vinecopulib/pyvinecopulib.git
WORKDIR /pyvinecopulib
RUN git checkout dev
RUN pip install .
WORKDIR /pyvinecopulib/docs
RUN pip install Sphinx sphinx_rtd_theme
CMD make html
```