Is there anything being done to make only or ifconfig *just work*? The single reason I went with sphinx in the first place was that I want to build a huge document with different tags enabled to strip features depending on the client.

After trying it out on a small demo, everything worked fine. But now, when moving to the actual documentation, the limitations of `only` and `ifconfig` pop out everywhere. I wanted to go with a very simple pattern:
```
.. only:: supportFeatureX

  .. include:: featureX.rst
```

However, each feature has its own section (and possibly subsection) so this just breaks apart. Is there anything I can do to make this work? I'm actually thinking about just adding an `#ifdef` and running it through a preprocessor before building, which feels really awkward. If this is something that can be achieved with an extension, I'd be happy to create one if pointed in the right direction.