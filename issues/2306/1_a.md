Harder to explain than to show.

If I have a section, followed by a section inside a true `.. only::` block, and follow that block with another section to which I've added my own label `.. _MyLinkName:`, I then get the warning 

> WARNING: undefined label: mylinkname (if the link has no caption the label must precede a section header)

first.rst: breaks with warning

```
=========
First one
=========

Heading before any only or tags
===============================

With some paragraph text.

.. only:: truetag

    This is a chunk that is in truetag.
    ===================================

    With some text under it

.. _MyLinkName:

Heading outside of truetag
==========================

This text refers to :ref:`MyLinkName`.
```

second.rst file: Leaving off the leading section (before the `only` block), then it works with no problems referencing  `MyLinkNameTwo`.

```
==========
Second one
==========

.. only:: truetag

    This is a chunk that is in truetag.
    ===================================

    With some text under it

.. _MyLinkNameTwo:

Heading outside of truetag
==========================

This text refers to :ref:`MyLinkNameTwo`.
```

Bundled the two rst files, index, and my conf.py file here in [testcase.zip](https://github.com/sphinx-doc/sphinx/files/123598/testcase.zip)

Fairly vanilla conf.py. Added `tags.add("truetag")` at the end to turn on the `.. only::` tag.

I'm not sure if this is the simplest test case that breaks.
