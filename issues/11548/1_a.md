**Is your feature request related to a problem? Please describe.**

When in a module you have the definition of a variable like a list or a dictionary with many items and maybe nested, a simple use of `autodoc` (or in my case `sphinx-automodapi` which is based anyway on that) results in a very bulky signature.

This would apply also to function or class methods, but my use-case is mainly related to module variables with long signatures.

**Describe the solution you'd like**

Is there a way to format it so that it shows every line separately?

**Describe alternatives you've considered**

In some SO posts I have found some artisanal solutions, but they didn't seem to work.

