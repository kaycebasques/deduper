I tested the option you mentioned, but that indeed works only on function-like objects (which is anyway weird to me, since a Python dictionary variable still belongs to the Python domain).

But that doesn't really do a "pretty" print, it would just squash the characters.