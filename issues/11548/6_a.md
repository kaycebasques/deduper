> I tested the option you mentioned, but that indeed works only on function-like objects (which is anyway weird to me, since a Python dictionary variable still belongs to the Python domain).

Signatures have no meaning for a variable content. A variable may have an annotation but a signature is a term reserved for functions. The option also works for type parameters list introduced by PEP 695.

> But that doesn't really do a "pretty" print, it would just squash the characters.

When you say "squash", what do you mean? for instance, lists with a lots of items should for instance only be printing their first and last ones? e.g., something like arrays are printed in numpy? One issue that I forsee is that the HTML output may still be ugly at the end because we don't for sure how large the page is. So even if we write `[a, ..., z]`, it might happen that it's still very ugly =/ 