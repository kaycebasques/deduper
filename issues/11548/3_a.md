Yes sorry, in this case, it's the content of the variable.

For example, a module variable which is a relatively long dictionary with items I want the user to see when taking a look at the reference API of my package becomes like the Code of Hammurabi when instead I would like to see it as I was using `pprint` or `json.dumps`.