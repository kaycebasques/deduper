> I think this might be difficult to define a prior - it might depend on the sphinx theme in use and/or the web page width for example.

Which is why I am reluctant to have it with some defaults. I think this should rather be an extension which can then be configured accordingly (and even have default configuration values depending on the theme for instance, or a set of predefined defaults, or use the python default values of pprint by default).

> I never did it, but it might be a fun side-project to learn something new - not sure about the time scale though

It is fun imo but yes, definitely time consuming if you don't know where to start or if you want to handle many corner cases.

Anyway, I'm away for now, so I'll leave the issue opened. However, I don't think it is something that will be included in the core library soon unless it's highly customizable (this specific feature needs to imho).


