Another alternative is to use [sphinxcontrib-apidoc](https://pypi.org/project/sphinxcontrib-apidoc/). I prefer this to `sphinx-immaterial` because it does not require one to group classes/functions, and automatically generates a hierarchical documentation for all files/modules in a package.