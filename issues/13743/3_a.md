In many cases an IDE can't figure out what type is returned by complex iterators. Thus a type hint in the code for the iteration variable is needed to assist the IDE and allow for auto completion.

Do you refer to those cases when you write about `# type: ...` or is it purely about the public interface e.g. of functions and methods?

The example described by me is needed for fluent coding, but not relevant for auto documentation of public interfaces.

Can you please clarify?

-----------------
```py
for node in graph.IterateActiveNodes(predicate=...):   # type: Node
   print(node)
```