@Paebbels I use PyCharm and have a similar problem, but often adding a line containing only an annotation above the iterator helps, eg in your example `node: Node`.

However, this wouldn't be affected -- I'm proposing removing the old-style type comment usage where function signatures were declared in a comment on the first line of the body. This is because Python 2 didn't support parameter annotations.

A