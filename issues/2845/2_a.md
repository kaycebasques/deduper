The error message leaking is a bug of Sphinx. I fixed it at     b69d373.

On the other hand, current implementation of numfig strongly depends on the captions.
So it is hard to support non captioned code-blocks soon.
