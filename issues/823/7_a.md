Did #6439 resolve the reproducibility concern raised in this thread? Download URLs still use a hash, but it is now derived from the source-relative path and should therefore be stable across build environments.

The original request for descriptive, non-hashed paths remains unresolved, but #6439 appears to limit the remaining scope of this issue to that request.