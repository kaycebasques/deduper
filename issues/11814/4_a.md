@jfbu 

> Actually I may have been wrong to add the `builder:latex` tag here ; definitely the question mentions LaTeX but it seems to make sense to HTML to. Am I wrong that also in HTML our `glossary` directive does not create back references?

Using the `html` builder generates a glossary, with forward-links throughout the document correctly linking to the glossary definitions, but there is zero back referencing (i.e. `html` build doesn't even generate the `index` page with back-references to the definitions nor back-references to the instances/uses of the glossary entries).

For what it's worth, I can live without this feature, but if it's straightforward to implement, so much the better (i.e. provides me a chance to use `rST`/Sphinx in place of raw LaTeX for certain technical documents). This issue was just a casual observation of mine.
