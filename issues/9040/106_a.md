So is this a correct summary of your thinking?

- There is clear demand for YAML configuration
- However, Python-specific config may be necessary _so often_ that allowing for YAML will just frustrate people who inevitably must switch.
- If this is the case, then only `conf.py` should be supported natively by Sphinx
- So, we should determine some indication of "how often people must use Python config anyway" and decide if that's strong enough to push one way or another
