I think non-coexistence *could* be an option if:

1. New configuration options are added to Sphinx for the common uses of Python-only features. Some examples already: https://github.com/sphinx-doc/sphinx/issues/9040#issuecomment-815105505 https://github.com/sphinx-doc/sphinx/issues/9040#issuecomment-818873536
1. A bit of research shows that very few **popular** extensions, configurations, etc. need/use Python-only features in `conf.py`, outside those added through the first bullet.

If that's the case, then users will indeed rarely have to switch format, in which case the downsides to non-coexistence are appropriately mitigated. The static file could then be the default format, since leaving `conf.py` as the default format doesn't serve the broader goal of expanding the universe of possible Sphinx users.

Without step 2, we're just theorizing about what users are likely to do, and making a decision purely on strength of belief.

**Update:** To clarify, if a project has a hugely complicated `conf.py`, we can assume that they are not the kind of user who would have started with a simple static file. Hence the word "popular" in step 2.

> In the same post an argument is made about how a YAML/TOML is almost valid Python already, and how replacing one character does the trick

It's the other way around - a Python file like the default `conf.py` that only does assignments of literal values is valid TOML. The following YAML (YAML has many syntax options, but the following is most common) is obviously not valid Python, and converting it would require learning Python's syntax:

```
mylist:
  - item1
  - item2
mydict:
  dk1: one
  dk2: two
```

----

I'll start a bit of my own research here. Any help will advance this issue:

* Off the top of my head, [recommonmark](https://github.com/readthedocs/recommonmark#autostructify) requires `def setup` to use `add_config_value` and `add_transform`. Thankfully, that extension is now deprecated.
* [Sphinx documentation](https://www.sphinx-doc.org/en/master/) (I haven't done a comprehensive review)
  *  [ifconfig](https://www.sphinx-doc.org/en/master/usage/extensions/ifconfig.html) asks users to define `def setup` and use `add_config_value`. I found [examples of use](https://github.com/search?q=%22sphinx.ext.ifconfig%22+filename%3Aconf.py+add_config_value+-confval1&type=Code) on GitHub (I filtered out some false positives).
* https://github.com/sphinx-contrib/
  * Any volunteer?
* [Awesome Sphinx](https://github.com/yoloseem/awesome-sphinxdoc/blob/master/README.rst)
  * Any volunteer? Some of the referenced projects are old though.
* [Framework :: Sphinx :: Extension](https://pypi.org/search/?q=&o=-created&c=Framework+%3A%3A+Sphinx+%3A%3A+Extension)
  * I haven't spent much time looking through these, but the few I looked at only require adding a string to `extensions` and only offer simple configuration options that can be expressed in YAML.
* [Framework :: Sphinx :: Theme](https://pypi.org/search/?q=&o=-created&c=Framework+%3A%3A+Sphinx+%3A%3A+Theme)
  * I haven't spent much time looking through these, but the few I looked at only require setting `html_theme = 'theme_name'` and only offer simple configuration options that can be expressed in YAML.
  * Old themes/versions asked users to do `html_theme_path = [sphinx_rtd_theme.get_html_theme_path()]`, but this can now be done by the theme itself with `app.add_html_theme` ([as documented](https://www.sphinx-doc.org/en/master/development/theming.html?highlight=%22def%20setup%22#distribute-your-theme-as-a-python-package)).
* GitHub code search
  * This does not seem like a quick way to discover common configurations. The search results are cluttered with [conf.py files](https://github.com/search?q=filename%3Aconf.py+add_config_value+-confval1+-value1+-AutoStructify+-m2r_parse_relative_links&type=Code) that are part of programming projects – so they aren't non-technical users.
* ReadTheDocs research
  * This can only be done by the RTD team, but I'm thinking perhaps they can filter the projects by "Programming Language: Only Words" and see what their `conf.py` files contain. If they are all simple assignments, then that is a good sign.