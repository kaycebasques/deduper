Ok lets get this back on track again.

So firstly, if sphinx can be configured by either `conf.py` or `sphinx.yaml`/`sphinx.toml` (+ optional `extension.py`), how are we detecting what to use?

1. Sphinx searches for these files automatically and errors if multiple are detected

or

2. You set specifically via `sphinx-build` what to look for, and possibly sphinx infers by the extension what format it is, e.g.: `sphinx-build --conf sphinx.yaml`