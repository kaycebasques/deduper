in `sphinx.builders.linkcheck.Hyperlink` there is a field called `lineno` with the type `Optional[int]`. There is another class `sphinx.builders.linkcheck.CheckResult` with the field `lineno` with type `int`.

in line 229, a `CheckResult` is constructed from a `Hyperlink`-

```python
for hyperlink in hyperlinks.values():
    if self.is_ignored_uri(hyperlink.uri):
        yield CheckResult(hyperlink.uri, hyperlink.docname, hyperlink.lineno, 'ignored', '', 0)
    ...
```

something is wrong here, just need to check which. Should the type of `Hyperlink.lineno` be `int`? Or should the type of 'CheckResult.lineno' be `Optional[int]`? Or should the types be the way they are now, but an exception raised if the `lineno` is `None`? 