Although the fragment content (`/type-concepts-advanced`) _looks like_ a path, it's likely that JavaScript loaded from the base URL (`https://microsoft.github.io/pyright/`) parses that and loads relevant content by parsing the fragment.

That said, we should still be able to get the `linkcheck` builder to pass on the base URL (it's a less strong guarantee that the link is correct, but better than indicating failure).

@rsokolewicz could you try configuring the [`linkcheck_anchors_ignore`](https://www.sphinx-doc.org/en/master/usage/configuration.html#confval-linkcheck_anchors_ignore) in your configuration to specify that URLs within `https://microsoft.github.io/pyright/` should not be anchor-checked?