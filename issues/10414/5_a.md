@AA-Turner Actually I am really confused how your example gets picked up by `FunctionDocumenter`. As far as I can tell all the predicates return False for it, just the same as our code:
```
In [2]: from sphinx.util import inspect

In [3]: from functools import wraps
   ...: 
   ...: def decorator(f):
   ...:     @wraps(f)
   ...:     def inner():
   ...:         return f()
   ...:     return inner
   ...: 
   ...: class Functor:
   ...:     """A functor."""
   ...: 
   ...:     def __call__(self, a, b, **kwargs):
   ...:         return a + b
   ...: 
   ...: f = Functor()

In [4]: member =  decorator(f)

In [5]: inspect.isfunction(member)
Out[5]: False

In [6]: inspect.isbuiltin(member)
Out[6]: False

In [7]: inspect.isroutine(member)
Out[7]: False
```

In which case I don't see how `FunctionDocumenter.can_document_member` could pass. Are you using a different version?