`can_document_member()` method is only called if the module or class has nested members. In this case, you need to use `automodule` directive to reproduce the problem:

```
.. automodule:: repro
   :members:
```


This is a very difficult problem. If we handles all "callable" objects by `FunctionDocumenter`, all classes are represented as functions because they are all callable and the priority of `FunctionDocumenter` is higher than `ClassDocumenter`.

Now I don't have enough time to take a look this in details. So I'll take time for this (maybe this weekend). Please let me know if you have a good idea to determine the type of given object correctly :-)