@tk0miya I am not suggesting that `FunctionDocumenter` should handle all callable objects, and that is not the cause of the issue in this case. I am suggesting that `FunctionDocumenter` should handle `wrapped_f`&mdash;*which is a function*. At least in my mind the root problem is the call to `unwrap_all` in `isfunction`, which causes `isfunction` to return False for intputs that are functions. 