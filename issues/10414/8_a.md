https://github.com/sphinx-doc/sphinx/issues/10414#issuecomment-1115261056

From this -- the stdlib `inspect` doesn't regard `f` as a function. Is your argument that `f` isn't but `wrapped_f` is? (If so it might be easier to special-case).

A