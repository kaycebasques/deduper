@AA-Turner  Well, the first screenshot above is from the deployed docs generated with Sphinx 4.4, and running locally with `-vv` output (also 4.4) shows clearly that our `ufunc` wrappers are ending up with `DataDocumenter`
```
[autodoc] getattr(_, 'bitwise_and')
[autodoc] => <function implemented.<locals>.wrapper at 0x7f9d0a0dd440>
[app] emitting event: 'autodoc-process-signature'('data', 'cunumeric.bitwise_and', <function implemented.<locals>.wrapper at 0x7f9d0a0dd440>, {}, Non
[app] emitting event: 'autodoc-process-docstring'('data', 'cunumeric.bitwise_and', <function implemented.<locals>.wrapper at 0x7f9d0a0dd440>, {}, ['C
[autodoc] output:

.. py:data:: bitwise_and
   :module: cunumeric
   :value: <function implemented.<locals>.wrapper>
```

If I explicitly call all the relevant predicates in `FunctionDocumenter.can_document_member` they definitely are all False:
```
legate37 ❯ legate
WARNING: Disabling control replication for interactive run
Welcome to Legion Python interactive console
>>> from sphinx.util import inspect
>>> from cunumeric import bitwise_and as member
>>> inspect.isfunction(member)
False
>>> inspect.isbuiltin(member)
False
>>> inspect.isroutine(member)
False
>>> member.__wrapped__
<ufunc bitwise_and>
>>> type(member.__wrapped__)
<class 'cunumeric._ufunc.ufunc.binary_ufunc'>
```

Is there any other  information can provide you about this object that might help? Building/running `cunumeric` from source is not trivial, unfortunately, so it's probably easier for me to respond to your asks. If that's not feasible I will have to try to work up a standalone MRE but that will take more time. 

Edit: I am not sure how it would be materially different from the trivial decorator, but here is the actual `implemented` function in question:
```
def implemented(
    func: AnyCallable, prefix: str, name: str, *, reporting: bool = True
) -> CuWrapped:
    name = f"{prefix}.{name}"

    wrapper: CuWrapped

    if reporting:

        @wraps(func)
        def wrapper(*args: Any, **kwargs: Any) -> Any:
            location = find_last_user_frames(not runtime.report_dump_callstack)
            runtime.record_api_call(
                name=name, location=location, implemented=True
            )
            return func(*args, **kwargs)

    else:

        @wraps(func)
        def wrapper(*args: Any, **kwargs: Any) -> Any:
            return func(*args, **kwargs)

    wrapper._cunumeric_implemented = True

    return wrapper
```