```python
In [1]: from sphinx.util.inspect import isfunction

In [2]: from functools import wraps

In [3]: class Functor:
   ...:     def __call__(self, a, b):
   ...:         print(a, b)
   ...: 

In [4]: def decorator(f):
   ...:     @wraps(f)
   ...:     def wrapper(*args, **kw):
   ...:         return f(*args, **kw)
   ...:     return wrapper
   ...: 

In [5]: f = Functor()

In [6]: assert isfunction(f) is False

In [7]: import inspect

In [8]: assert inspect.isfunction(f) is False
```

shows the underlying problem.

A