Embarrased I didn't check that! Thanks!

We should probably change L63-64

https://github.com/sphinx-doc/sphinx/blob/bd4b19451c9697b3deec687b8e4c893c3916ad81/sphinx/util/inspect.py#L53-L70

to something like:

```python
         elif inspect.isroutine(obj) and hasattr(obj, '__wrapped__'): 
             if hasattr(obj.__wrapped__, '__call__'):
                 return obj  # Don't unwrap wrapped callable types
             obj = obj.__wrapped__  # type: ignore 
```

Could you submit a PR with this change? This is a desk review so I don't know if it would break tests, etc (we should add a test for the new behaviour if we adopt it).

A