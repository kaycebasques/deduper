Perhaps I've misunderstood, but I actually can't reproduce:

`conf.py`
```python
import os, sys
sys.path.insert(0, os.path.abspath('.'))
extensions = ['sphinx.ext.autodoc']
```

`repro.py`
```python
from functools import wraps

def decorator(f):
    @wraps(f)
    def inner():
        return f()
    return inner

class Functor:
    """A functor."""

    def __call__(self, a, b, **kwargs):
        return a + b

f = Functor()
wrapped_f = decorator(f)
```

`index.rst`
```restructuredtext
.. autofunction:: repro.f

.. autofunction:: repro.wrapped_f
```

This yields:

![image](https://user-images.githubusercontent.com/9087854/166312467-8ea7e9da-0961-4995-9e6b-9c15810e04c2.png)

A