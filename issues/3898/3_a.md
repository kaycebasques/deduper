Note:

* In `add_object_type()`, `sphinx.domains.std.GenericObject` is used to create a directive. It is strongly coupled with standard domain.
* We have to update `Domain.get_object()` to export object_types which is registered by `add_object_type_to_domain()` as objects.

I feel this is not simple work. So I marked this as "help wanted" :-)