As @chrisjsewell observed in https://github.com/sphinx-doc/sphinx/issues/12536#issuecomment-2221292659, there is no way to reference things in ``intersphinx`` `objects.inv` unless you have the object/extension loaded in your local build.

This doesn't seem ideal -- but fixing would require serialising much more data, on first thought. Capturing this issue for more discussion.

A