Hi @gvanem -- please don't describe something in an entirely volunteer maintained project as "awful".

`--no-color` calls `sphinx.util.console.nocolor()`, whereas `NO_COLOR=1` prevents `sphinx.util.console.color_terminal()`. I am happy to review a PR that you work on rectifying this.

A