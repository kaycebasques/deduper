It would be great for localized documentation to optionally also change the filename (and possibly directory structure).

It might make sense to specify the desired filename on top of the included files.  Maybe as part of a once-per-page directive.

If given pointers, I might be able to implement it myself.