I traced this to `_format_signatures()` leaving `signatures` empty for data objects, then the signature event handler assigning `signatures[0]`. I was thinking of adding a regression case for a callable data object with an annotated class variable, then guarding the empty-signature path with the smallest change that preserves the existing behavior. The issue looks unclaimed and I do not see an open PR. Would a focused fix along those lines fit Sphinx's expectations?