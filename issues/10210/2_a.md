The following change on the 4.x branch in the text writer, as seen in the html writer, fixes the issue:

```diff
diff --git a/sphinx/writers/text.py b/sphinx/writers/text.py
index 9fef050c1..80ab52b90 100644
--- a/sphinx/writers/text.py
+++ b/sphinx/writers/text.py
@@ -1047,9 +1047,7 @@ class TextTranslator(SphinxTranslator):
         pass

     def visit_number_reference(self, node: Element) -> None:
-        text = nodes.Text(node.get('title', '#'))
-        self.visit_Text(text)
-        raise nodes.SkipNode
+        self.visit_reference(node)

     def visit_download_reference(self, node: Element) -> None:
         pass
```

However, I'm not sure if it doesn't cause problems elsewhere.