> It would be nice if toctree had an option to produce multiple columns (for HTML output), like the hlist directive.

What do you mean by 'nicer'? Some rationale would be useful. As @picnixz said, this seems to be a styling problem (for which we would accept a documentation patch).

A