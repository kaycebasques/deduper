What about the css trick in the issue you linked? does it now fail?

Personnally, I think it should be fine but then can you do something for LaTeX outputs as well? (I think the feature should be respected for LaTeX and man but that should probably be another PR).