>With these changes, the output is much cleaner looking, but roughly 40
>percent of the screen on the right is wasted (solid color), as shown below:

I think `body_max_width = none` for `html_theme_options` will help you. If not, you need to make a your own theme.

>Note also the size of the heading "combinatorics.py".  The corresponding
>line of html looks like this:

I don't know how you are marking up it. But sphinx does not consider it is a heading.

Note: Sphinx considers the following marked up text is a heading
```
heading title
=============
```
