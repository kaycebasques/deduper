To not loose the qualifier content when using screenreaders, I think it requires either aria-label="NAME (qualifier)" in the a tag or move the span tag inside the <a> tag and then style-out the link emphasis 