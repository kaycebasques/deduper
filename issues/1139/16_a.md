What I essentially want is this: In the `env-updated` stage I want to inspect the frontmatter (metadata) of the processed docs and then generate _virtual docs_ based on that. It would be convenient if I could re-run the build up to that point for those newly injected docs and then have a sort of second `env-updated` event. This stage would be convenient because it is still before references are resolved, so an extension can create/modify the pending references if necessary and let them get resolved at a later stage.

I hope this is somewhat understandable. I believe fundamentally it is the same issue. What works for now for my use case is generating files into the source directory that are then picked up and processed in a subsequent build... which is not a great solution.