@Cadair I ended up getting quite far with this too, iirc by creating a `class VirtualFileInput(SphinxFileInput)`. In my case I wanted to inject virtual documents/data at specific times of the build, namely after `env_updated`. So in my case I had to inject the "virtual files" there and then basically trigger a second build in `build_finished` if there were any virtual docs.

I let it be for a while because life got busy but I think if I revisit it, I can perhaps get to the point of releasing an extension specifically for dynamic data/document injection. However I think this issue is still relevant as I would prefer a public/straight forward API for this instead of messing with internals. So, to me, such an event would be welcome, yes.