I found myself here after hacking on [sphinx-tags](https://github.com/melissawm/sphinx-tags), and looking for a way of injecting new files into the build process which also works on incremental builds. [It's very possible I have missed some way of achieving this but I can't spot it.]

Currently sphinx-tags generates a new folder of files in the `builder-inited` step, which works for the first build, but if a source file is changed and its tags list modified, then this doesn’t get picked up in an incremental build. Using the `env-get-outdated` step I can modify existing tag files (https://github.com/melissawm/sphinx-tags/pull/122), but if a new tag which doesn't already have a rst file generated for it is added then I can't see a way to pick up this file.

Would a new event to modify file collection be welcome?