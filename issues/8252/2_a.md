Hi, I'm triaging the issue backlog. I just discovered that [`:file:` role](https://www.sphinx-doc.org/en/master/usage/restructuredtext/roles.html#role-file) already exists.  It seems like it should be possible to preserve the existing behavior when a directive is not provided for a given file, and otherwise do the linking when the file is documented via a directive. But there's an existing feature related to placeholder text that might complicate things. E.g. the `{x}` will be displayed differently from the rest of the text to indicate that it's a placeholder:

```
:file:`/usr/lib/python3.{x}/site-packages`
```

