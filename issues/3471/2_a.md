At present, the status of `suppress_warnings` is experimental. So it is still in develop and unstable.
We are adding the types and sub-types each time we received issues.

1. Yes, I'd designed `ref.*` is used for reference-warnings (including (mainly?) missing-reference). About `any` ref, it is not simply added yet.
2. Simply not updated yet. In addition, as you saw, I'm not good at English. So updating document is always at behinds.
3. There are no proper way to customize the message. Is that needed? As a adhoc way, you can customize through extensions or i18n message catalogs.
4. Not considered yet. It might be better to use more deeper typing models (cf. `domains.py.foobar`)

As described above, the rule of warning-types is not finalized yet.
So we should consider it carefully. If you are interested, please give us idea :-)