Apologies if I'm just bringing up known issues. 

I'm playing around with the logging module (and mostly loving it!) but it's led me into some documentation holes, slippery semantics, and potential issues.

Here are the ref types cited in the documentation for ``suppress_warnings`` with the corresponding ``dangling_warnings`` definition:

type.subtype | definition
------------ | --------------
ref.term     | ``'term not in glossary: %(target)s'``
ref.ref      | ``'undefined label: %(target)s (if the link has no caption the label must precede a section header)'``
ref.numref   | ``'undefined label: %(target)s'``
ref.keyword  | ``'unknown keyword: %(target)s'``
ref.option   | ``'unknown option: %(target)s'``
ref.citation | ``'citation not found: %(target)s'``
ref.doc      | ``'unknown document: %(target)s'``

1. All of the current ``ref.*`` types suggest that ref == missing-reference, but there's at least one other reference-related warning (when there's more than one valid target for an ``any`` ref) that isn't getting passed with a type/subtype. This may be a minor namespace issue?
2. The doc for ``suppress_warnings`` doesn't mention that all missing refs will get passed with a type of 'ref' and a subtype of '<rolename>'.
3. I don't see any documentation about how domains can customize missing-ref messages with the ``dangling_warnings`` dict.
4. Should the domain be included in the specifier (in the type, in the subtype, or as a separate entity) since multiple domains can implement the same roles?
