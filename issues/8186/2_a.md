Right, there is no preprocessor so you will need to expand the macro manually (to ``constexpr`` I guess). However, in this case I think it would work with adding ``Q_DECL_CONSTEXPR`` as a custom attribute ID to parser via the [``cpp_id_attributes``](https://www.sphinx-doc.org/en/master/usage/configuration.html#confval-cpp_id_attributes) config var.