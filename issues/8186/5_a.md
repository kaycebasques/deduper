Indeed. This trick only works if the macro is in a position where an attribute could be (and we have changed the parser to accept attributes there).
I'm not familiar with Qt and the ``Q_DECLARE_METATYPE`` macro, but from https://doc.qt.io/qt-5/qmetatype.html I'm not sure if it should be in the declarations Sphinx gets.