Right, for the ``PORTGROUP`` example the attribute trick doesn't work, as it is used to "disguise" the type of the variable (I think). For ``Q_DECL_DEPRECATED_X`` I don't know, you example seems incomplete.
It would help a lot if you could provide the information requested in the bug report template, and preferably an example project for reproducing the situation.