As with ``PORTGROUP`` and ``Q_DECL_DEPRECATED_X``, the declaration is here obfuscated by the ``DEFINE_CAFFE_CPU_UNARY_FUNC`` macro (and the declaration seems to have been cut off early). Even wrose, this is a project-specific macro.
I don't know if you use a meta-compiler to produce the Sphinx declarations (like Breathe), but my best suggestion is to somehow give Sphinx the actual declaration you would like to document, without non-attribute macros.