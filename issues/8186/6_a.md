Unfortunately I found some more. PORTGROUP and Q_DECL_DEPRECATED_X is added to cpp_paren_attributes. 
Code looks like:
```
class cPowStation : public cStation
{
  STATIONDECL(cPowStation);
  cPowStation();

  PORTGROUP(float64) m_i1;
  cOPort<float64> m_o1;
  /**
  @sparam{exponent, Exponent of power operation., 2}
  */
  sint32 m_exponent;

  void pow(cBufferT<float64> *i_buffer);
};
```

```
If the function has no return type:
  Error in declarator or parameters-and-qualifiers
  Invalid C++ declaration: Expecting "(" in parameters-and-qualifiers. [error at 24]
    PORTGROUP (float64) m_i1
    ------------------------^
If the function has a return type:
  Error in declarator or parameters-and-qualifiers
  If pointer to member declarator:
    Invalid C++ declaration: Expected identifier in nested name. [error at 24]
      PORTGROUP (float64) m_i1
      ------------------------^
  If declarator-id:
    Invalid C++ declaration: Expected identifier in nested name. [error at 24]
      PORTGROUP (float64) m_i1
```


```
If the function has no return type:
  Error in declarator or parameters-and-qualifiers
  Invalid C++ declaration: Expected identifier in nested name. [error at 20]
    Q_DECL_DEPRECATED_X ("Use cObj::read; Will be removed in 11.10") bool readObj(cObj &obj
    --------------------^
If the function has a return type:
  Invalid C++ declaration: Expected identifier in nested name. [error at 20]
    Q_DECL_DEPRECATED_X ("Use cObj::read; Will be removed in 11.10") bool readObj(cObj &obj
    --------------------^
```