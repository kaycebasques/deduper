I guess the implementer of the math directive did not know about the `:name:` option is commonly used in the reST world. So there is no special reason. So I think we have to migrate to standard `:name:` option. FYI: https://github.com/sphinx-doc/sphinx/issues/5010.

Note: AFAIK, docutils' math directive was added after Sphinx's. So they're different implementations.