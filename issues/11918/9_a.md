@puck Ok; yep, got it - I wanted to make sure I had the correct understanding.  To confirm:

Currently it's not possible to link to an individual entry on the `genindex.html` page, because the entries there do not have anchors.

(each _letter_ has an anchor - so it's currently possible to link to [Sphinx index entries beginning with `A`](https://www.sphinx-doc.org/en/master/genindex.html#A), for example - but not to individual items)

You're not asking for _every_ entry to have an anchor -- but your expectation was that the `.. index` directive's `name` option should create an anchor in the index.

(although I think it _might_ be OK simply to create anchors for every entry in `genindex.html`, that might have unexpected side-effects and so perhaps it's easier to start by allowing individual anchors)