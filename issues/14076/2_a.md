It is possible to reproduce the problem by translating the documentation where this problem originally appeared. The following commands can be used to do that:

```
python -mvenv ~/pyvenvs/sphinxtest
. ~/pyvenvs/sphinxtest/bin/activate
git clone https://github.com/LivingLogic/LivingLogic.Python.xist.git
cd LivingLogic.Python.xist
git checkout vsql
pip install -e .
pip install Sphinx sphinxcontrib.jquery pygments pygll cherrypy oracledb
cd docs
make clean html
```