@etrian-dev I'm having some difficulty replicating the problem - I've created a project with an index, two glossary files in a `glossary/` subdirectory, and have copied your provided example contents into those -- but I don't end up with duplicated `#` characters in the `singlehtml` output, either from v9.0.4 or from v9.1.0 - I'll keep trying, though.