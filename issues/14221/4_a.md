Hi. This was noticed as part of the build of a large document that uses `singlehtml`, and does not show issues with Sphinx `8.1.3`. After upgrading to `8.2.3` we noticed the problem, but I was able to confirm that it persisted even after that; while I didn't try `8.1.3` on this example, I would expect it to produce correct output, so I would say it is a regression that occurred (or was uncovered by other modifications such as the ones in the aforementioned issue) between `8.1.3` and `8.2.3`.