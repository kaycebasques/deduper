> If there's a better way to do this, I'd love to hear about it.

The easiest solution is putting the last comment `# -- end setup` on the last line of the function definition having the function's indentation level.

I think this FR should be posted on the Black code formatter side, it's essentially the same problem as https://github.com/psf/black/issues/3566 and as it's caused by Black it makes a lot more sense for Black to have an option to not introduce spurious blank lines.