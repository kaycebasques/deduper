A small concern I had about adding support for embedded SVGs is that there is no implicit namespace for each SVG file embedded within an HTML document.  This can cause collisions between the `id` values of separate SVG images within a document.  It seems that the `docutils` folks are aware of this, though - it's mentioned in the relevant [commit message](https://sourceforge.net/p/docutils/code/9517/).