Thank you for reporting. But it is difficult to detect what module the partial object is defined. So autodoc checks the original function to detect where it is defined. I agree this is unexpected behavior. So it would be nice if somebody sends a PR to fix it.

Thanks,