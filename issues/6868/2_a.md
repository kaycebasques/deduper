+0: I feel natural to use `index` filename for directory index. It is well known keyword on web.
-0: It is a bit confusable if we have both `intro.rst` and `intro/index.rst`.
-0: `master_doc` can change the filename of root document. But this behavior ignores it. I'd not like to add a magic keyword to Sphinx. But using `master_doc` instead of `.../index` is not good to me.

IMO, this is good idea. But it is difficult to keep balance with current behavior.