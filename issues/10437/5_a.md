> * doing `pip install -e .[test,docs]` and advise also building Sphinx own docs as extra test

After running this, `make test` worked.  This is also mentioned after the `tox` section, but maybe it should be mentioned earlier.  In the "Getting started" section, at step 5, it suggest to run `pip install -e .`, but this doesn't include the tests and their dependencies.  Perhaps an option to install everything could be mentioned there.

> * mention that on Unixen `make test` works to launch the test suite

I would also mention `pytest -v`, which should work on all platforms and it might be simpler/more familiar to users (especially compared to the `make test TEST=...` syntax).

IOW, from this section I would expect, in order:
1. whether I need to install any additional dependencies, and the command to install them
2. a simple command to run all the tests (such as `pytest -v` or `make test`)
3. more advanced commands to run specific or additional tests
4. alternative options such as `tox` (note that even after `pip install -e .[test,docs]`, `tox` is still not installed)