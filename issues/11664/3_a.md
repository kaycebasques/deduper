The code is https://github.com/matplotlib/matplotlib/blob/main/doc/api/axes_api.rst

I can imagine the following solution (but there may be other ways to achive this):

Add a `conf.py` setting
```
override_link_target = {
    "matplotlib.axes.Axes": "other_location"
    # not sure whether the key must be the class name or some associated rst link concept.
    # what I want to express is: the target of :class:`matplotlib.axes.Axes` should go to "other_location"
}
```
where I simply define the label
```
.. other_location:
```
anywhere else in the code.

