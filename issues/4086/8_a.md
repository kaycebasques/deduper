> Or, an alternative to that whole proposal would be to just add the ':' to the html output, it would force everyone to adopt that format, but it's an improvement for them anyway, right? ;o)

Technically this would be the fix for this bug, to simply make the html output consistent with the latex one.

Supporting customizable captions as you proposed in the previous comment would be considered a separate feature?