+1, I guess this should be trivial to correct?

Also I came across a bug: if you change only the 'figure' format with `numfig_format={'figure': 'Figure %s'}`, and also have Tables for example in the document, you get this:
```
Exception occurred:
  File "/usr/lib/python2.7/dist-packages/sphinx/writers/html.py", line 279, in append_fignumber
    self.body.append(prefix % numsstr + ' ')
TypeError: not all arguments converted during string formatting
The full traceback has been saved in /tmp/sphinx-err-PjF2Qy.log, if you want to report the issue to the developers.
```
Specifying all formats fixed the problem:
```
numfig_format = {'figure': 'Figure %s', 'table': 'Table %s',
                 'code-block': 'Listing %s', 'section': 'Section %s'}
```