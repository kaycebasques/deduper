I also see it in PDF files. It would be nice to reset the current nesting level with a directive like

```rst
.. toc-level-up:: 2
```

The current way how `toctree` works, limits how to decompose bigger documentations into multiple files and pages. Sometimes one needs to add hundreds of lines into one file, just to have a well-structured navigation bar, but the ReST file becomes hard to maintain.
