@kratsg when I use `make_mode` (`-M`) locally, I do receive output about broken links to stderr (and also the `_build/linkcheck` directory is created again).  Is there any chance `pixi` or some other aspect of your job configuration is consuming/suppressing the stderr output?