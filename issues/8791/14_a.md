@kratsg hm, interesting - when I attempted the build, I used `sphinx-build -q -b linkcheck docs docs/_build`.  In make-mode (`-M` instead of `-b`), the `linkcheck` directory is indeed created - so, my mistake.  I'll take a look into the other differences in `make_mode` -- perhaps that could be a factor.