> [@kratsg](https://github.com/kratsg) I think the problem there could be the `rm -r _build/linkcheck` step -- the linkchecker doesn't create a directory named that, so the exit code of `1` is probably from that command failing (previously the GitHub Actions job had `continue-on-error: true` set if I understand correctly, so that may not have caused a visible failure).

Retrying the job and it succeeds -- it does create the directory (at least when I run things locally -- it does). Using the following command

```
sphinx-build -M linkcheck docs docs/_build
```

I see

```
$ tree -d docs/
docs/
├── _build
│   ├── doctrees
│   │   └── _generated
│   └── linkcheck
├── _generated
├── _static
│   └── css
├── abstracts
└── bib

10 directories
```

So I think it has something more to do with a temporal/random issue with external link access.