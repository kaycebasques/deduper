> Also, there is a `output.json` file which you can process with a script if you want.

Thanks a stack. Pruning the file before a link checker run to get rid of previous items, we have been able to apply this `jq`-based command successfully to get precisely informed about all failing items. 🍀

```shell
cat .build/output.json | jq -s '.[] | select(.status=="broken" or .status=="timeout")'
```
