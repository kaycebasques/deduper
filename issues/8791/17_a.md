> > Is there any chance `pixi` or some other aspect of your job configuration is consuming/suppressing the stderr output?
> 
> I don't see how. I would be shocked if `pixi` suppresses it, it's just running the commands within an env. Is there any flag that would suppress warnings or stderr output from sphinx-build? Or a way to test it? If I drop the `-q`, I see the typical stdout.

@kratsg from the [`sphinx-build` manual](https://www.sphinx-doc.org/en/master/man/sphinx-build.html), neither `-M`, nor `-q` options that you're specifying should prevent printout of warnings/errors.

You could try adding `--warning-file <path>` to explicitly write (tee, I suppose) the same output elsewhere in addition?