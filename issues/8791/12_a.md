@kratsg I think the problem there could be the `rm -r _build/linkcheck` step -- the linkchecker doesn't create a directory named that, so the exit code of `1` is probably from that command failing (previously the GitHub Actions job had `continue-on-error: true` set if I understand correctly, so that may not have caused a visible failure).