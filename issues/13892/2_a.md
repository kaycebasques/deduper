Thanks @cglukas - I think this is a really good find; I can replicate locally that searching the Sphinx self-built documentation for `search -adapters` _does_ return results including the word `adapters`.

(NB: I am not a maintainer here, but have contributed to and have an interest in the HTML search functionality)

I have one or two questions:

> This means you can actually redefine the word splitting in your `conf.py`. If you follow the how to reproduce, you will see that after tweaking the `splitQuery` function, the `Search.performTermsSearch` function will actually get the excluded words (if you use the browser debugger 😅).

If I understand this correctly, we do use the excluded terms (e.g. the word `adapters` from the `-adapters` input) to perform a document search retrieval, even after the changes proposed in your current PR (up to 80d6ad1b938b117484db6f6beb75a0a221c2cb24)?

...and if so: does that mean that in theory there is an opportunity to optimise by _not_ retrieving those documents at all?

> Great! But now comes the bug. If you exclude one word from the search, the entire search will abort after finding one page with the excluded word. I would expect that the search will just filter out any exclusions, but it actually just does a hard break (sphinx/themes/basic/static/searchtools.js:648). Thereby, it will miss most of the relevant search results which renders the exclusion mechanism useless.

When I attempted to replicate this (from eaebbec4d165efcb5048fc06431e3bf796c9d224), I did receive more than one search result containing an excluded term -- so maybe there is additional subtletly here?

Another way to phrase that: when I search for `search adapters` I receive 12 search results, and when I search for `search -adapters` I also receive 12 search results (I expected fewer for the second query, based on the bugreport, so if possible I'd like us to try to figure out what is going on there).

<img width="1792" height="970" alt="Image" src="https://github.com/user-attachments/assets/a7d0d19a-2ee7-4a86-9b4a-8661d4b06d93" />