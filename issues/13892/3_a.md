Hi @jayaddison,

thanks for your quick reply. I try to answer your questions in this post:

> If I understand this correctly, we do use the excluded terms (e.g. the word adapters from the -adapters input) to perform a document search retrieval, even after the changes proposed in your current PR (up to https://github.com/sphinx-doc/sphinx/commit/80d6ad1b938b117484db6f6beb75a0a221c2cb24)?

No, the updated `splitQuery` will correctly mark the excluded words. This means that we are (hopefully) only searching for the correct search terms.

>  does that mean that in theory there is an opportunity to optimise by not retrieving those documents at all?

I don't know. Maybe if the excluded word is matched in the title, we could remove the file from the full text search. It could be an option. I did not really look into this too deeply. I wanted to fix the found bug, rather than introducing more by optimizing 😅.

>Another way to phrase that: when I search for search adapters I receive 12 search results, and when I search for search -adapters I also receive 12 search results (I expected fewer for the second query, based on the bugreport, so if possible I'd like us to try to figure out what is going on there).

Did you adjust the split query part in your conf.py as described in the "How to reproduce section"? Because the default search without any modification will search for "search" and "adapters" in both queries. The whole "excluded term" concept is implemented but excluded terms never reach the logic because the split query will only produce a list of "words":

You can even try this in the developer console on the official sphinx search:
https://www.sphinx-doc.org/en/master/search.html?q=search+-adapters
<img width="243" height="107" alt="Image" src="https://github.com/user-attachments/assets/0a320e17-a1e5-4a78-8215-21cb3a76151d" />

And because of that, both searches will use the exact same words and return the same results.