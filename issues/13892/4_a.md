> Did you adjust the split query part in your conf.py as described in the "How to reproduce section"? Because the default search without any modification will search for "search" and "adapters" in both queries. The whole "excluded term" concept is implemented but excluded terms never reach the logic because the split query will only produce a list of "words":

I have to admit that I did not, no.  After reading your initial bugreport, I felt that it was likely that this affected default configurations also (e.g. without modifying `conf.py` to customise `splitQuery`) -- and after confirming that to be the case, I delved into other aspects of the bug and pull request.