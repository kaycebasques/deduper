Hi @tk0miya,
I'm still slightly confused by this parameter. What *types* can the `types` be? Is this restricted to an iterable of primitive types? Or do they need to be *callables*/ have constructors? Is it possible to extend these to validate custom types? Is it possible to use `typing` types, and if not, is support planned for these? Would it make sense to accept a `typing.Union` instead of an iterable?