> I'm no LaTeX expert, but I'm pretty sure you can (and probably should) manually wrap that equation in the markup. In my experience the `math::` directive is a "with great power comes great responsibility" situation more often than not.

It's not Latex thing. Actually, in html pages, the paragraphs got auto-wrapped by default (reducing the page width and words will be sent to the next line), I was just wondering why the equations stay unchanged as if they were size-fixed images. 