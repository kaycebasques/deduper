IMHO, it makes sense to explain `html_context` with a small example, but not to link to `html_css_files`, because similar misconceptions can occur between many config variables and linking between all of them would create a mess. And yes, PRs are targeted to `master` :-)