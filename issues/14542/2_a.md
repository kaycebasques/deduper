Thanks for filing a bugreport - I have two questions about this:

- It is valid for Unix filenames to contain `#` and `?` characters - so, for example, a filename of `Who watches the watchers?.pdf` is valid.  Is it an acceptable risk that we might break some existing file links, or make it difficult to add certain local files?
- As far as I am aware, the querystring part of the URL is only relevant for webpages that are retrieved remotely -- in comparison to the fragment part that web browsers may use when opening local HTML files.  Is support for both required?