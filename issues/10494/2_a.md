This problem still exists and I have almost exactly the same setup (using [`sphinxcontrib-confluencebuilder`](https://sphinxcontrib-confluencebuilder.readthedocs.io/en/stable/) to build and post to Confluence documentation for multiple Python packages).

Seemingly one improvement since this issue was originally opened is that I get an explicit error:
> ERROR: Invalid target URI value `'<base_confluence_url>'` in intersphinx_mapping['set_A'][0]. Target URIs must be unique (other instance in intersphinx_mapping['set_B']). 

instead of a subtle, silent failure.

However, like the original poster, I think this should be a perfectly valid setup