@ArthurAttout can you confirm whether when you wrote that output contains ``HELLO`` and ``WORLD`` you really meant with uppercase and in that case tell us which version of DocUtils is used.  I could not reproduce this and if really it happens that for some version of DocUtils that class names are not lowercased, this means a breakage at least for LaTeX container support.