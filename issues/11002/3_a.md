> Not satisfactory, I guess. I replaced `\uppercase` by `\MakeUppercase` else the macro contents would not be uppercased.

Actually, using ``..raw:: latex`` directive to directly inject `\begin{customizedrequirement}{what}{ever}{you}{want}{here}` and `\end{customizedrequirement}` is perhaps less cumbersome than my convoluted proposal.  You don't need container directive then.  My convoluted proposal makes an effort for an interface where one only modifies what needs to be modified at the spot.  But if one stars using ``..raw:: latex`` then why bother with a container.