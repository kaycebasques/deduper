Hello @jfbu, thanks for the quick response. 

Indeed, the class names are in **lowercase** in my output. That's actually something I noticed way later, which seemed odd to me at that time, but since it's intentional, I guess it's fine. This lowercasing causes some separate problems on my end. 

As for your suggestion to use `raw:: latex` indeed, it bypasses any need to wrap the content in a  `container`. I wanted to fiddle around and see if a built-in, pure `.rst` solution existed for my need, but it doesn't look like it. 

Like I specified,  I'm currently using a  python script to patch the generated `.tex`. I'll probably tune it further to generate `raw:: latex` directives around the content, like you suggested. This should achieve exactly the same behaviour. 

Feel free to close this request if you feel it won't be implemented. I'm aware the original request was very LaTeX-focused. 

Thanks for the support ! 