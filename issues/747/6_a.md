@tk0miya - Thank you very much for this snippet. It serves my need, with one small addition. I had to also add `kwargs.setdefault('anchorlinenos', True)` in the `highlight_block` method. 

Previously, I attempted to set the `html_formatter` of the `PygmentsBridge` class to a custom one, which would set `anchorlinenos` to `True`, but I couldn't find an easy way to set `lineanchors`. Your suggestion is much better. Thanks again. 
