(whoops, see edited comment)

It may even make sense as it is to just replace `ul`s with `ol`s and use `list-style-type: disc` so that the list still reads as an ordered list. HTML elements are meant to represent the document's content, after all.