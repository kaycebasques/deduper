> `"None"` is not converted to Python `None`.

I used `None` in my example to use a real example but I was expecting this. I suppose that's another issue, that would be only relevant if there were some situations where `None` vs. an empty string produces a different result.