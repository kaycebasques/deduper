> To fix the issue it would take some design to support that.

Maybe this issue should be closed once the docs are updated, without additional code? I think that `--conf-dir` is more appropriate for nested overrides?