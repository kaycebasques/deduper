Small diagnosis from the code path: `sphinx.ext.napoleon.docstring.NumpyDocstring._parse()` is source-order driven, so the Attributes/Methods sections themselves don't seem to be re-sorted inside napoleon. That makes me suspect the misordering is happening when autodoc merges class docstrings with member docstrings, rather than in the section parser. If that's right, the fix likely belongs in the class/member concatenation path, not in `_parse_attributes_section()` / `_parse_methods_section()`.