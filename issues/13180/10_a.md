Fixed in PR #14559 (https://github.com/sphinx-doc/sphinx/pull/14559).

`NumpyDocstring.``_parse()` now reorders sections for class docstrings so that Attributes and Methods come right after Parameters, matching numpydoc 1.8 conventions.

3 new tests verify the reordering behavior and confirm no regression for non-class docstrings.