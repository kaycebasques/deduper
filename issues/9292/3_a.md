Allowing the regex to be greedy on the name part fixes my issues and doesn't appear to cause any problems. So I believe it should be

```python
r'(?x)(.+)\s+(\S+)\s+(-?\d+)\s+?(\S*)\s+(.*)'
```

Will run tests and make a PR...