The symlink aspect makes this a good canonical-path regression test. I would expect the test to build a tiny docs tree where a toctree points at a symlinked document, then assert that Sphinx 8 does not report the realpath target as a separate orphaned document under `-W`.
