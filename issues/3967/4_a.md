Unfortunately, I don't think I can rely on disabling smartquotes with `docutils.conf`.  If the user has a `~/.docutils` file enabling smartquotes, I don't think there is any way for my project to disable it.  At least, that's my reading of http://docutils.sourceforge.net/0.7/docs/user/config.html . But the better solution would be to return to the original Sphinx setup where smartquotes was in the writers, rather than in the doctree parsing.