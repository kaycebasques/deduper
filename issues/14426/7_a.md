`Sopel` experienced this failure downstream when evaluating our 3.15 support (see https://github.com/sopel-irc/sopel/issues/2737) and I can confirm that as of `rc1` it is no longer there.

I think this issue is probably ripe for closure now that CPython has issued a fix, @FriedrichFroebel can you re-test?