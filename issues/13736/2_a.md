Hi, 

1. If you set `SKIP_LATEX_BUILD` in the environment, the latex tests should be skipped.  Could you try this?
2. TeXLive 2021 is definitely not too old.  Unfortunately I can't test on my device currently, but for example a complete TeXLive 2019 is expected to be compatible with current Sphinx.  Perhaps you have only a partial installation (possibly some font missing).
3. Check [here](https://www.sphinx-doc.org/en/master/usage/builders/index.html#sphinx.builders.latex.LaTeXBuilder) what we currently say about requirements.  Please report if this looks useful enough or more precision is needed. I am not certain *The testing is currently (since 5.3.0) done on Ubuntu 22.04LTS* is currently accurate.  It may be outdated.
4. I would be interested into the actual cause of failure.  You may be able to fetch the suitable `.log` file in a subdirectory of the `pytest` temporary directory.