> Hi, 
> 
> 1. If you set `SKIP_LATEX_BUILD` in the environment, the latex tests should be skipped.  Could you try this?

More precisely it skips all tests doing a pdf build, but this is probably what OP requested.

Apart from that I wonder if luatex is installed in the OP device. What does `lualatex sample2e` report?
