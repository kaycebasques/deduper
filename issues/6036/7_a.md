+1 for this improvement, file-relative line numbers rather than docstring-relative numbers would make fixing doc warnings a lot easier :)

At the moment when I get a warning on docstring line 57 I first have to manually find the associated docstring in my file, then count down 57 lines (or use an editor that shows line numbers and do some mental arithmetic :D )

> Note: It seems hard to fix this completely. Because the docstring might be modified by extensions (via `autodoc-process-docstring` event). For example, napoleon extension rewrites whole of docstring.
True, but I think that problem also applies to the docstring-relative line numbers we're currently displaying. I think evitably users need be tolerant of the numbers occasionally being slightly off. 

However it'd still make a big different if the line number quoted was relative to start of file rather than start of docstring. Looks like this is possible e.g. https://stackoverflow.com/questions/11608747/extract-line-numbers-of-all-docstrings and/or maybe the ModuleAnalyzer can help (maybe for data members at any rate)