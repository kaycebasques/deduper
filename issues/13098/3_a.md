> > ...
> > **Describe alternatives you've considered**
> > ...
> > * The [`JSON.rawJSON`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/rawJSON) function was also considered; this resolves the concerns with `JSON.parse`.  However: it appears not to be widely-deployed yet (it is not available in Firefox or Safari, for example.  It became available in Chrome 114 and Edge 114, both released ~Jun 2023).
> 
> Self-correction: `JSON.rawJSON` appears to be limited to primitive types; it [cannot construct arrays or objects](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/rawJSON#using_json.rawjson_to_create_json_expressions_of_different_types) (in fact, this is highlighted by some bold text in the introduction on that page, too. I missed both of those previously).

There is a `tc39` (ECMAscript technical committee) proposal for a `JSON.parseImmutable(...)` function that appears relevant, though: https://github.com/tc39/proposal-json-parseimmutable