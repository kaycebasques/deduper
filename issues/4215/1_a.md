I have a website with Sphinx that has both German and English articles. I distinguish them by having document meta information at the top:

```
:date: 2017-11-03
:lang: de
```

Then in my template I can make use of this `lang` attribute and do various things.

In the toctrees all over my site, I would like to expose the language to the user by the use of a unicode flag (🇩🇪 and 🇺🇸). This means that in a `toctree` directive, the titles of the document are prefixed with `🇺🇸 ` if there is no `lang` attribute and `🇩🇪 ` when it is set to `de`.

I have looked through the code a bit, `TocTreeCollector.process_doc.build_toc` has the line

    nodetext = visitor.get_entry_text()

where I think I might be able to add this. But I am not sure whether and how I can access the document meta data at this point in the code.

Is my idea feasible to implement? If so, could you please give me some pointers what I would have to do?