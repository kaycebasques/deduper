I can implement this. The approach is clear:

1. Add `source_language` config option (default "en")
2. Set `lang` attribute on untranslated nodes in `AddTranslationClasses`
3. Override `starttag` in HTML5Translator to respect node `lang`

/assign