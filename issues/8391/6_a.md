@jayaddison would you be interested in looking into this? [httpx](https://github.com/encode/httpx/) may be a good candidate as I believe it looks to keep the interface of Requests.

Alternativley, we create a new ``sphinx.util._async_http`` helper module instead of replacing ``util.requests``.

To François' point above, I don't know how [AIO-HTTP](https://github.com/aio-libs/aiohttp) has developed w.r.t. authentication in the last three years.

A