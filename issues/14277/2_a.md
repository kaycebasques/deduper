I'm noticing in the profile results that 60% of the time is spent in `toctree.py:468(_toctree_copy)`, around half of which is caused by `toctree.py:102(_resolve_toctree)` with the following line:

https://github.com/sphinx-doc/sphinx/blob/cc7c6f435ad37bb12264f8118c8461b230e6830c/sphinx/environment/adapters/toctree.py#L207

Note that `newnode` is freshly created in this method, and (as far as I can see) not otherwise relevant. 
If I'm not misinterpreting, I think this means that we're spending around 30% of the build time copying a new node to prune it.

I've created a mockup to avoid the copy by pruning in-place. Currently testing if it works:

https://github.com/sphinx-doc/sphinx/compare/master...Ivorforce:sphinx:prune-dont-copy