@SilverRainZ I assume, you have the write all option activated, otherwise using the cached doctree only writes modified pages, but doesn't update the navigation bar if headlines were changed.

I also suggest to build without cache on `main` branch, otherwise certain error messages and warnings won't be shown in incremental builds.