As mentioned in #9561, a duplicate issue, a workaround is to give the xref an explicit title, e.g. `` :ref:`Section A <a>` `` or `` :doc:`Describe Some Page Here <some-page>` ``. That is less ideal for translated docs, since each heading must hard-code the title instead of resolving it.