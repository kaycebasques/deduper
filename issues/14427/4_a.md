> what would you expect?

In previous versions, it showed just the title, with an underline (see `additional context` in the top post), which was fine. Even if not clickable, it still told the user that it is something important that they might want to look up (and those references are clickable in the online version of the documentation).
Those `<#other-ref>` and `<>` however are pointless and meaningless, and just a confusing waste of space.