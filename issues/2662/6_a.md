It looks like the relevant code here originates from `docutils` ([ref](https://github.com/docutils/docutils/blob/b768e2626088711dec257b0847b563d02700a712/docutils/docutils/writers/_html_base.py#L1516-L1535)), and in fact the assertion has been removed recently (within the past month).

If we have a way to repro the cause of the assertion case, that could be useful to help check whether `docutils` has sufficient test coverage for it.

Also: we should probably consider refreshing the HTML5 writer code at some point to incorporate improvements from `docutils`.  That could require some careful work though (our code diverged from upstream a little, so the differences would need to be reconciled).