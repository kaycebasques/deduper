Well,
you know from the very beginning what fails. That looks a bit like unit testing, i.e. you check the smallest condition.

In my opinion it would be also great to print out some kind of info to have some guess about what went wrong. I mean, I looked at the log, but it just gave me a 20 line stack trace....what to do with that? 

Exceptions may not be what you want because you want Sphinx to stop, so one possibility is ot first print out some information and then rise an assert:

```
if not assert_condition:
   print some meaningful info
   assert(False)
```

or the like. I mean, If I make a mistake using the API I would like to have a guess before digging down the core API to put print myself! 
