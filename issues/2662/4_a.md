I think the mechanism you're looking for is `assert ..., "some meaningful info"`.

IMO the [source](https://github.com/sphinx-doc/sphinx/blob/master/sphinx/writers/html.py#L210) is unreadable enough that any way of making it clearer would be welcome.  I'm not sure splitting up the assert is useful either since it seems to be some larger assertion that's being done there (I don't understand it enough to judge);  adding an error message does not hurt though.
