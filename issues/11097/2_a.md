There is also an option [latex_documents](https://www.sphinx-doc.org/en/master/usage/configuration.html#confval-latex_documents), but I never saw a good example of that; I need only one final pdf document, not several ones with different authors etc.
And I had to create the largest header ("Welcome to...") because of this issue https://github.com/readthedocs/sphinx-autoapi/issues/302 .
And one more thing about [Markdown](https://www.markdownguide.org/basic-syntax/#headings) hierarchy: '#' corresponds to ```<h1>``` header, which should be the top one. I can't understand why it becomes a subsection then.