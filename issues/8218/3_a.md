Yeah, parsing the signature was never a well defined operation, as `__repr__` isn’t required to generate valid Python code.

To be reliable, `sphinx.ext.autodoc` needs to use something like [`typing.get_type_hints`](https://docs.python.org/3/library/typing.html#typing.get_type_hints), and construct the signature itself instead of trying to parse it.