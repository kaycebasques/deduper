Further debugging this, I've found that `type Sample` requires `.. autotype:: Sample` and `py:type`, while `Sample: TypeAlias` requires `.. autoclass:: Sample` and `py:class`.

It seems the real issue may stem from the fact that when used as a parameter or return type, `py:class` is used even for `type Sample`.