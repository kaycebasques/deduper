This is an expected behaviour.

You need to include the inherited members as an autodoc option since otherwise <code>:meth:\`x\`</code> is actually referencing ``Child.x``. Since the inherited members are not shown by default, a reference to `Child.x` will obviously fail to render. 