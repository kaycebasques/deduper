Could you elaborate on prolonging the build times? The copying should be fairly quick. The problem with symlinks is that static files could be templates, so we can't unconditionally copy the whole directory.

I'd also want to ensure anything like this works properly on all supported platforms (including Windows).

A