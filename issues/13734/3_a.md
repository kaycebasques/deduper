Actually, the build time turned out to be an issue with some of my code. But I still believe having two copies of the same assets isn't desirable. 

On some thought, I think linking the entire file may not be the best solution, but is there any way that the copying function respects symlinks in the original _static directory? Because as it stands, if there is a symlink in the _static folder, then all the contents in the link are also copied over.

As for the issue with Windows, is it not sufficient to make this into a configuration key that can be toggled? Furthermore, I think symbolic links can be created in Windows 11 without administrator privileges, but require permissions in earlier versions.