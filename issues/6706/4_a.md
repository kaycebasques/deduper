No, `:alt:` and `:align:` have effected to other formats. For example, text and manpage builders uses `:alt:` option to render a text instead of the image. And LaTeX builder uses `:align:` option to layout the image in the output.