Yes this would only be for HTML outputs like `html`, `dirhtml`, `singlehtml`, `json` etc. As I understand though there are already some `image` directive options that are only applicable to HTML output like `:alt:` and `:align:`.