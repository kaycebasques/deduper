> Would you be interested in proposing a PR to change it?

I'm happy to help you with the JavaScript integration that generates the flyout if somebody else builds the HTML layout and CSS styles for the positioning (I'm not really good at that and it could take me forever). [I've already done this work with the Read the Docs theme](https://github.com/readthedocs/sphinx_rtd_theme/pull/1526) re-using the existing HTML layout and CSS styles, so it shouldn't be too hard.