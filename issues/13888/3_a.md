I noticed this during packaging documentation of a python library as Debian package. I had to remove the empty directory after building the documentation, because it is normally not wanted to keep empty directories in the package.