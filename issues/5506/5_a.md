Just stumbled across this issue. I agree with the original poster. If my toctree changes, all pages should be re-built to fix their sidebars. 
I am no expert, but maybe instead of re-building every time index.rst change, only rebuild if the information in the `.. toctree` directive changes. 
This would reduce unnecessary rebuilds. 