> Hmm? Is the configuration related to the datatemplate extension? I can understand and agree with you if you're saying about Sphinx should raise an error for the wrong variable reference in the custom templates (including HTML themes).

The reason I don't want to enable it for the HTML templates, is because the HTML templates are not written with this jinja2 behavior in mind.

Hence, for compatibility, there is a need to configure what sort of SandboxedEnvironment() a directive/plugin should see.
