Because sphinx core creates the SandboxedEnvironment(), but near as I can tell, sphinx core does not allow for any way to configure SandboxedEnvironment().

Ideally, I could configure the SandboxedEnvironment() for each builder(and even each directive) from within conf.py